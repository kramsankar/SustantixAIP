window.AIP_V217_AUDIT={release:'v2.17',baseline:'v2.16',area:'Work Order Intelligence',uiOnly:true,excelBusinessDataChanged:false,rootCause:'The old bordered .ops-kpi Work Order renderer remained physically executable as a fallback while accumulated sidebar/navigation layers could still reach it on first entry. Sub-tab clicks used the later stable wo213 renderer, producing the observed border/color change.',changes:['Permanently disabled the legacy Work Order renderer body so it can never emit .ops-kpi KPI cards','Added capture-phase ownership of the Work Order left-nav entry and routed it directly through the stable renderer','Removed any stale legacy KPI container after every stable render','Added one final borderless white wo213 KPI contract with fixed per-KPI bar colors','Preserved the same six KPI definitions and all Work Order sub-tabs']};