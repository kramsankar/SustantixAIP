window.AIP_V216_AUDIT={release:'v2.16',baseline:'v2.15',area:'Work Order Intelligence',uiOnly:true,excelBusinessDataChanged:false,rootCause:'Core renderFns captured the legacy Work Order renderer before the later stable renderer was installed; first navigation therefore used the captured renderer while sub-tab clicks used the stable renderer.',changes:['Changed renderFns.workorderintelligence from a captured function reference to a dynamic stable-renderer delegation','Added an explicit Work Order branch in the core activate() router that invokes the stable renderer on every entry, even when the view was previously marked rendered','Requires six wo213 KPI cards before marking Work Order Intelligence rendered','Applied one final borderless white KPI contract with fixed per-KPI bar colors and no transitions/animations']};