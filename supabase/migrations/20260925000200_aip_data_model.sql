-- Sustantix AIP · governed data model from AIP_Data_v732.xlsx (sha256 bc89da203530…)
-- Generated by tools/schema — do not edit by hand.

-- Work Orders · Maintenance & Reliability · business key: Work_Order_ID
create table if not exists aip."work_orders" (
  tenant_id uuid not null references aip.tenants(id) on delete cascade,
  row_key text not null,
  "work_order_id" varchar(200),
  "maintenance_type" varchar(200),
  "asset_id" varchar(200),
  "plant_id" varchar(200),
  "asset_tag" varchar(200),
  "description" varchar(1000),
  "priority" varchar(200),
  "status" varchar(200),
  "created_date" timestamp without time zone,
  "sla_due" timestamp without time zone,
  "sla_hours" integer,
  "actual_resolution_hours" numeric(24,2),
  "sla_result" varchar(200),
  "source" varchar(200),
  "estimated_cost" numeric(24,2),
  "assigned_technician_id" varchar(200),
  "assigned_crew" varchar(200),
  "pm_frequency_days" integer,
  "pm_due_date" date,
  "actual_pm_completion_date" date,
  "pm_compliance_tolerance_days" integer,
  "pm_compliance_result" varchar(200),
  "pm_overdue_days" integer,
  currency char(3) not null default 'INR' check (currency ~ '^[A-Z]{3}$'),
  created_at timestamptz not null default now(),
  updated_at timestamptz not null default now(),
  primary key (tenant_id, row_key)
);
comment on table aip."work_orders" is 'Workbook sheet "Work Orders"';
alter table aip."work_orders" enable row level security;
drop policy if exists p_read on aip."work_orders";
create policy p_read on aip."work_orders" for select to authenticated using (aip.has_role(tenant_id));
drop policy if exists p_write on aip."work_orders";
create policy p_write on aip."work_orders" for all to authenticated
  using (aip.has_role(tenant_id, array['planner','admin'])) with check (aip.has_role(tenant_id, array['planner','admin']));
drop trigger if exists t_audit on aip."work_orders";
create trigger t_audit after insert or update or delete on aip."work_orders" for each row execute function aip.audit_row();
drop trigger if exists t_touch on aip."work_orders";
create trigger t_touch before update on aip."work_orders" for each row execute function aip.touch_updated_at();

-- Cost & Rate Master · Core · business key: Rate_Key
create table if not exists aip."cost_and_rate_master" (
  tenant_id uuid not null references aip.tenants(id) on delete cascade,
  row_key text not null,
  "rate_key" varchar(200),
  "decision_category" varchar(200),
  "cost_component" varchar(200),
  "basis" varchar(200),
  "rate_or_fixed_value" numeric(24,2),
  "unit" varchar(200),
  "applies_to" varchar(200),
  "governance_note" varchar(200),
  created_at timestamptz not null default now(),
  updated_at timestamptz not null default now(),
  primary key (tenant_id, row_key)
);
comment on table aip."cost_and_rate_master" is 'Workbook sheet "Cost & Rate Master"';
alter table aip."cost_and_rate_master" enable row level security;
drop policy if exists p_read on aip."cost_and_rate_master";
create policy p_read on aip."cost_and_rate_master" for select to authenticated using (aip.has_role(tenant_id));
drop policy if exists p_write on aip."cost_and_rate_master";
create policy p_write on aip."cost_and_rate_master" for all to authenticated
  using (aip.has_role(tenant_id, array['planner','admin'])) with check (aip.has_role(tenant_id, array['planner','admin']));
drop trigger if exists t_audit on aip."cost_and_rate_master";
create trigger t_audit after insert or update or delete on aip."cost_and_rate_master" for each row execute function aip.audit_row();
drop trigger if exists t_touch on aip."cost_and_rate_master";
create trigger t_touch before update on aip."cost_and_rate_master" for each row execute function aip.touch_updated_at();

-- OEM Warranty Learning · Maintenance & Reliability · business key: OEM_Learning_ID
create table if not exists aip."oem_warranty_learning" (
  tenant_id uuid not null references aip.tenants(id) on delete cascade,
  row_key text not null,
  "oem_learning_id" varchar(200),
  "asset_class" varchar(200),
  "oems" varchar(200),
  "policy_recommendation_count" integer,
  "active_warranty_count" integer,
  "remaining_coverage" numeric(24,2),
  "claim_count" integer,
  "claimed" numeric(24,2),
  "approved_recovery" numeric(24,2),
  "evidence_readiness_pct" numeric(24,2),
  "learning_action" varchar(200),
  "governance_rule" varchar(200),
  currency char(3) not null default 'INR' check (currency ~ '^[A-Z]{3}$'),
  created_at timestamptz not null default now(),
  updated_at timestamptz not null default now(),
  primary key (tenant_id, row_key)
);
comment on table aip."oem_warranty_learning" is 'Workbook sheet "OEM Warranty Learning"';
alter table aip."oem_warranty_learning" enable row level security;
drop policy if exists p_read on aip."oem_warranty_learning";
create policy p_read on aip."oem_warranty_learning" for select to authenticated using (aip.has_role(tenant_id));
drop policy if exists p_write on aip."oem_warranty_learning";
create policy p_write on aip."oem_warranty_learning" for all to authenticated
  using (aip.has_role(tenant_id, array['planner','admin'])) with check (aip.has_role(tenant_id, array['planner','admin']));
drop trigger if exists t_audit on aip."oem_warranty_learning";
create trigger t_audit after insert or update or delete on aip."oem_warranty_learning" for each row execute function aip.audit_row();
drop trigger if exists t_touch on aip."oem_warranty_learning";
create trigger t_touch before update on aip."oem_warranty_learning" for each row execute function aip.touch_updated_at();

-- PNO_Interventions · Planning & Optimization · business key: Intervention_ID
create table if not exists aip."pno_interventions" (
  tenant_id uuid not null references aip.tenants(id) on delete cascade,
  row_key text not null,
  "intervention_id" varchar(200),
  "work_order_id" varchar(200),
  "plant_id" varchar(200),
  "asset_id" varchar(200),
  "asset_tag" varchar(200),
  "asset_class" varchar(200),
  "maintenance_type" varchar(200),
  "intervention_description" varchar(200),
  "priority" varchar(200),
  "risk_score_pct" numeric(24,2),
  "planning_as_of" varchar(200),
  "required_by" varchar(200),
  "planned_start" varchar(200),
  "duration_hours" integer,
  "required_skill" varchar(200),
  "required_crew_qty" integer,
  "required_part_id" varchar(200),
  "required_part_qty" integer,
  "required_tool_id" varchar(200),
  "vehicle_class" varchar(200),
  "permit_required" varchar(200),
  "outage_required" varchar(200),
  "planning_status" varchar(200),
  "source_record" varchar(200),
  "source_sheet" varchar(200),
  "calculation_basis" varchar(1000),
  "planned_finish" varchar(200),
  "actual_start" varchar(200),
  "actual_duration_hours" integer,
  "actual_completion" varchar(200),
  "erp_execution_status" varchar(200),
  "actual_source" varchar(200),
  created_at timestamptz not null default now(),
  updated_at timestamptz not null default now(),
  primary key (tenant_id, row_key)
);
comment on table aip."pno_interventions" is 'Workbook sheet "PNO_Interventions"';
alter table aip."pno_interventions" enable row level security;
drop policy if exists p_read on aip."pno_interventions";
create policy p_read on aip."pno_interventions" for select to authenticated using (aip.has_role(tenant_id));
drop policy if exists p_write on aip."pno_interventions";
create policy p_write on aip."pno_interventions" for all to authenticated
  using (aip.has_role(tenant_id, array['planner','admin'])) with check (aip.has_role(tenant_id, array['planner','admin']));
drop trigger if exists t_audit on aip."pno_interventions";
create trigger t_audit after insert or update or delete on aip."pno_interventions" for each row execute function aip.audit_row();
drop trigger if exists t_touch on aip."pno_interventions";
create trigger t_touch before update on aip."pno_interventions" for each row execute function aip.touch_updated_at();

-- PNO_Resources · Planning & Optimization · business key: Resource_ID
create table if not exists aip."pno_resources" (
  tenant_id uuid not null references aip.tenants(id) on delete cascade,
  row_key text not null,
  "resource_type" varchar(200),
  "resource_id" varchar(200),
  "resource_name" varchar(200),
  "crew_id" varchar(200),
  "region_or_home_site" varchar(200),
  "primary_skill_or_type" varchar(200),
  "skill_level" varchar(200),
  "certification_or_control" varchar(200),
  "valid_to" date,
  "availability" varchar(200),
  "capacity_or_payload" varchar(200),
  "fuel_type" varchar(200),
  "ghg_boundary" varchar(200),
  "solar_ipp_context" varchar(200),
  "source" varchar(200),
  created_at timestamptz not null default now(),
  updated_at timestamptz not null default now(),
  primary key (tenant_id, row_key)
);
comment on table aip."pno_resources" is 'Workbook sheet "PNO_Resources"';
alter table aip."pno_resources" enable row level security;
drop policy if exists p_read on aip."pno_resources";
create policy p_read on aip."pno_resources" for select to authenticated using (aip.has_role(tenant_id));
drop policy if exists p_write on aip."pno_resources";
create policy p_write on aip."pno_resources" for all to authenticated
  using (aip.has_role(tenant_id, array['planner','admin'])) with check (aip.has_role(tenant_id, array['planner','admin']));
drop trigger if exists t_audit on aip."pno_resources";
create trigger t_audit after insert or update or delete on aip."pno_resources" for each row execute function aip.audit_row();
drop trigger if exists t_touch on aip."pno_resources";
create trigger t_touch before update on aip."pno_resources" for each row execute function aip.touch_updated_at();

-- PNO_Resource_Calendar · Planning & Optimization · business key: Crew_ID + Month
create table if not exists aip."pno_resource_calendar" (
  tenant_id uuid not null references aip.tenants(id) on delete cascade,
  row_key text not null,
  "crew_id" varchar(200),
  "month" varchar(200),
  "gross_capacity_hours" integer,
  "planned_hours" integer,
  "available_hours" integer,
  "utilization_pct" numeric(24,2),
  "basis" varchar(200),
  "calendar_deduction_hours" integer,
  "effective_available_hours" integer,
  "ot_cap_hours" integer,
  "ot_available_hours" integer,
  "effective_capacity_with_ot_hours" integer,
  "capacity_basis" varchar(1000),
  created_at timestamptz not null default now(),
  updated_at timestamptz not null default now(),
  primary key (tenant_id, row_key)
);
comment on table aip."pno_resource_calendar" is 'Workbook sheet "PNO_Resource_Calendar"';
alter table aip."pno_resource_calendar" enable row level security;
drop policy if exists p_read on aip."pno_resource_calendar";
create policy p_read on aip."pno_resource_calendar" for select to authenticated using (aip.has_role(tenant_id));
drop policy if exists p_write on aip."pno_resource_calendar";
create policy p_write on aip."pno_resource_calendar" for all to authenticated
  using (aip.has_role(tenant_id, array['planner','admin'])) with check (aip.has_role(tenant_id, array['planner','admin']));
drop trigger if exists t_audit on aip."pno_resource_calendar";
create trigger t_audit after insert or update or delete on aip."pno_resource_calendar" for each row execute function aip.audit_row();
drop trigger if exists t_touch on aip."pno_resource_calendar";
create trigger t_touch before update on aip."pno_resource_calendar" for each row execute function aip.touch_updated_at();

-- PNO_Materials · Planning & Optimization · business key: Intervention_ID
create table if not exists aip."pno_materials" (
  tenant_id uuid not null references aip.tenants(id) on delete cascade,
  row_key text not null,
  "requirement_id" varchar(200),
  "intervention_id" varchar(200),
  "plant_id" varchar(200),
  "asset_id" varchar(200),
  "material_id" varchar(200),
  "material_description" varchar(200),
  "required_qty" integer,
  "unit" varchar(200),
  "stock_on_hand" integer,
  "available_qty" integer,
  "material_status" varchar(200),
  "need_by" varchar(200),
  "earliest_receipt" varchar(200),
  "lead_time_days" integer,
  "criticality" varchar(200),
  "preferred_supplier" varchar(200),
  "storage_location" varchar(200),
  "replenishment_mode" varchar(200),
  "value_type" varchar(200),
  "source" varchar(200),
  created_at timestamptz not null default now(),
  updated_at timestamptz not null default now(),
  primary key (tenant_id, row_key)
);
comment on table aip."pno_materials" is 'Workbook sheet "PNO_Materials"';
alter table aip."pno_materials" enable row level security;
drop policy if exists p_read on aip."pno_materials";
create policy p_read on aip."pno_materials" for select to authenticated using (aip.has_role(tenant_id));
drop policy if exists p_write on aip."pno_materials";
create policy p_write on aip."pno_materials" for all to authenticated
  using (aip.has_role(tenant_id, array['planner','admin'])) with check (aip.has_role(tenant_id, array['planner','admin']));
drop trigger if exists t_audit on aip."pno_materials";
create trigger t_audit after insert or update or delete on aip."pno_materials" for each row execute function aip.audit_row();
drop trigger if exists t_touch on aip."pno_materials";
create trigger t_touch before update on aip."pno_materials" for each row execute function aip.touch_updated_at();

-- PNO_Fleet_Schedule · Planning & Optimization · business key: Intervention_ID
create table if not exists aip."pno_fleet_schedule" (
  tenant_id uuid not null references aip.tenants(id) on delete cascade,
  row_key text not null,
  "intervention_id" varchar(200),
  "work_order_id" varchar(200),
  "plant_id" varchar(200),
  "asset_id" varchar(200),
  "asset_tag" varchar(200),
  "asset_class" varchar(200),
  "priority" varchar(200),
  "planning_status" varchar(200),
  "planned_start" varchar(200),
  "planned_finish" varchar(200),
  "duration_hours" integer,
  "assigned_crew" varchar(200),
  "assigned_technician_id" varchar(200),
  "required_by" varchar(200),
  "source" varchar(200),
  "actual_start" varchar(200),
  "actual_duration_hours" integer,
  "actual_completion" timestamp without time zone,
  "erp_execution_status" varchar(200),
  "actual_source" varchar(200),
  "aip_optimized_start" varchar(200),
  "aip_optimized_duration_hours" integer,
  "aip_optimized_finish" varchar(200),
  "approved_start" varchar(200),
  "approved_duration_hours" integer,
  "approved_finish" varchar(200),
  "schedule_path" varchar(200),
  created_at timestamptz not null default now(),
  updated_at timestamptz not null default now(),
  primary key (tenant_id, row_key)
);
comment on table aip."pno_fleet_schedule" is 'Workbook sheet "PNO_Fleet_Schedule"';
alter table aip."pno_fleet_schedule" enable row level security;
drop policy if exists p_read on aip."pno_fleet_schedule";
create policy p_read on aip."pno_fleet_schedule" for select to authenticated using (aip.has_role(tenant_id));
drop policy if exists p_write on aip."pno_fleet_schedule";
create policy p_write on aip."pno_fleet_schedule" for all to authenticated
  using (aip.has_role(tenant_id, array['planner','admin'])) with check (aip.has_role(tenant_id, array['planner','admin']));
drop trigger if exists t_audit on aip."pno_fleet_schedule";
create trigger t_audit after insert or update or delete on aip."pno_fleet_schedule" for each row execute function aip.audit_row();
drop trigger if exists t_touch on aip."pno_fleet_schedule";
create trigger t_touch before update on aip."pno_fleet_schedule" for each row execute function aip.touch_updated_at();

-- PNO_Constraints · Planning & Optimization · business key: Constraint_ID
create table if not exists aip."pno_constraints" (
  tenant_id uuid not null references aip.tenants(id) on delete cascade,
  row_key text not null,
  "constraint_id" varchar(200),
  "constraint_name" varchar(200),
  "constraint_class" varchar(200),
  "rule" varchar(200),
  "evidence_sheet" varchar(200),
  "status" varchar(200),
  "priority_order" integer,
  created_at timestamptz not null default now(),
  updated_at timestamptz not null default now(),
  primary key (tenant_id, row_key)
);
comment on table aip."pno_constraints" is 'Workbook sheet "PNO_Constraints"';
alter table aip."pno_constraints" enable row level security;
drop policy if exists p_read on aip."pno_constraints";
create policy p_read on aip."pno_constraints" for select to authenticated using (aip.has_role(tenant_id));
drop policy if exists p_write on aip."pno_constraints";
create policy p_write on aip."pno_constraints" for all to authenticated
  using (aip.has_role(tenant_id, array['planner','admin'])) with check (aip.has_role(tenant_id, array['planner','admin']));
drop trigger if exists t_audit on aip."pno_constraints";
create trigger t_audit after insert or update or delete on aip."pno_constraints" for each row execute function aip.audit_row();
drop trigger if exists t_touch on aip."pno_constraints";
create trigger t_touch before update on aip."pno_constraints" for each row execute function aip.touch_updated_at();

-- PNO_Scenarios · Planning & Optimization · business key: Scenario_ID
create table if not exists aip."pno_scenarios" (
  tenant_id uuid not null references aip.tenants(id) on delete cascade,
  row_key text not null,
  "scenario_id" varchar(200),
  "scenario_name" varchar(200),
  "description" varchar(200),
  "crew_capacity_multiplier" numeric(24,2),
  "cost_weight_multiplier" numeric(24,2),
  "weather_threshold_pct" integer,
  "cost_cap" numeric(24,2),
  "scenario_state" varchar(200),
  currency char(3) not null default 'INR' check (currency ~ '^[A-Z]{3}$'),
  created_at timestamptz not null default now(),
  updated_at timestamptz not null default now(),
  primary key (tenant_id, row_key)
);
comment on table aip."pno_scenarios" is 'Workbook sheet "PNO_Scenarios"';
alter table aip."pno_scenarios" enable row level security;
drop policy if exists p_read on aip."pno_scenarios";
create policy p_read on aip."pno_scenarios" for select to authenticated using (aip.has_role(tenant_id));
drop policy if exists p_write on aip."pno_scenarios";
create policy p_write on aip."pno_scenarios" for all to authenticated
  using (aip.has_role(tenant_id, array['planner','admin'])) with check (aip.has_role(tenant_id, array['planner','admin']));
drop trigger if exists t_audit on aip."pno_scenarios";
create trigger t_audit after insert or update or delete on aip."pno_scenarios" for each row execute function aip.audit_row();
drop trigger if exists t_touch on aip."pno_scenarios";
create trigger t_touch before update on aip."pno_scenarios" for each row execute function aip.touch_updated_at();

-- PNO_Optimization · Planning & Optimization · business key: Scenario_ID + Intervention_ID
create table if not exists aip."pno_optimization" (
  tenant_id uuid not null references aip.tenants(id) on delete cascade,
  row_key text not null,
  "scenario_id" varchar(200),
  "intervention_id" varchar(200),
  "plant_id" varchar(200),
  "asset_id" varchar(200),
  "priority" varchar(200),
  "criticality_score" integer,
  "value_score" numeric(24,2),
  "readiness_pct" integer,
  "estimated_cost" numeric(24,2),
  "optimization_score" numeric(24,2),
  "rank" integer,
  "calculation_basis" varchar(200),
  currency char(3) not null default 'INR' check (currency ~ '^[A-Z]{3}$'),
  created_at timestamptz not null default now(),
  updated_at timestamptz not null default now(),
  primary key (tenant_id, row_key)
);
comment on table aip."pno_optimization" is 'Workbook sheet "PNO_Optimization"';
alter table aip."pno_optimization" enable row level security;
drop policy if exists p_read on aip."pno_optimization";
create policy p_read on aip."pno_optimization" for select to authenticated using (aip.has_role(tenant_id));
drop policy if exists p_write on aip."pno_optimization";
create policy p_write on aip."pno_optimization" for all to authenticated
  using (aip.has_role(tenant_id, array['planner','admin'])) with check (aip.has_role(tenant_id, array['planner','admin']));
drop trigger if exists t_audit on aip."pno_optimization";
create trigger t_audit after insert or update or delete on aip."pno_optimization" for each row execute function aip.audit_row();
drop trigger if exists t_touch on aip."pno_optimization";
create trigger t_touch before update on aip."pno_optimization" for each row execute function aip.touch_updated_at();

-- PNO_Governance · Planning & Optimization · business key: Intervention_ID
create table if not exists aip."pno_governance" (
  tenant_id uuid not null references aip.tenants(id) on delete cascade,
  row_key text not null,
  "governance_id" varchar(200),
  "intervention_id" varchar(200),
  "decision_state" varchar(200),
  "decision_owner" varchar(200),
  "decision_date" integer,
  "decision_basis" varchar(200),
  "policy_id" varchar(200),
  created_at timestamptz not null default now(),
  updated_at timestamptz not null default now(),
  primary key (tenant_id, row_key)
);
comment on table aip."pno_governance" is 'Workbook sheet "PNO_Governance"';
alter table aip."pno_governance" enable row level security;
drop policy if exists p_read on aip."pno_governance";
create policy p_read on aip."pno_governance" for select to authenticated using (aip.has_role(tenant_id));
drop policy if exists p_write on aip."pno_governance";
create policy p_write on aip."pno_governance" for all to authenticated
  using (aip.has_role(tenant_id, array['planner','admin'])) with check (aip.has_role(tenant_id, array['planner','admin']));
drop trigger if exists t_audit on aip."pno_governance";
create trigger t_audit after insert or update or delete on aip."pno_governance" for each row execute function aip.audit_row();
drop trigger if exists t_touch on aip."pno_governance";
create trigger t_touch before update on aip."pno_governance" for each row execute function aip.touch_updated_at();

-- PNO_POLICY · Planning & Optimization · business key: Policy_ID
create table if not exists aip."pno_policy" (
  tenant_id uuid not null references aip.tenants(id) on delete cascade,
  row_key text not null,
  "policy_id" varchar(200),
  "domain" varchar(200),
  "rule" varchar(200),
  "value" varchar(200),
  "unit" varchar(200),
  "value_type" varchar(200),
  "source" varchar(200),
  "config_status" varchar(200),
  "version" varchar(200),
  "effective_from" date,
  created_at timestamptz not null default now(),
  updated_at timestamptz not null default now(),
  primary key (tenant_id, row_key)
);
comment on table aip."pno_policy" is 'Workbook sheet "PNO_POLICY"';
alter table aip."pno_policy" enable row level security;
drop policy if exists p_read on aip."pno_policy";
create policy p_read on aip."pno_policy" for select to authenticated using (aip.has_role(tenant_id));
drop policy if exists p_write on aip."pno_policy";
create policy p_write on aip."pno_policy" for all to authenticated
  using (aip.has_role(tenant_id, array['planner','admin'])) with check (aip.has_role(tenant_id, array['planner','admin']));
drop trigger if exists t_audit on aip."pno_policy";
create trigger t_audit after insert or update or delete on aip."pno_policy" for each row execute function aip.audit_row();
drop trigger if exists t_touch on aip."pno_policy";
create trigger t_touch before update on aip."pno_policy" for each row execute function aip.touch_updated_at();

-- PNO_Scheduling_Rules · Planning & Optimization · business key: Rule_ID
create table if not exists aip."pno_scheduling_rules" (
  tenant_id uuid not null references aip.tenants(id) on delete cascade,
  row_key text not null,
  "rule_id" varchar(200),
  "rule_name" varchar(200),
  "rule_type" varchar(200),
  "rule_value" varchar(200),
  "unit" varchar(200),
  "governance_status" varchar(200),
  "applies_to" varchar(200),
  created_at timestamptz not null default now(),
  updated_at timestamptz not null default now(),
  primary key (tenant_id, row_key)
);
comment on table aip."pno_scheduling_rules" is 'Workbook sheet "PNO_Scheduling_Rules"';
alter table aip."pno_scheduling_rules" enable row level security;
drop policy if exists p_read on aip."pno_scheduling_rules";
create policy p_read on aip."pno_scheduling_rules" for select to authenticated using (aip.has_role(tenant_id));
drop policy if exists p_write on aip."pno_scheduling_rules";
create policy p_write on aip."pno_scheduling_rules" for all to authenticated
  using (aip.has_role(tenant_id, array['planner','admin'])) with check (aip.has_role(tenant_id, array['planner','admin']));
drop trigger if exists t_audit on aip."pno_scheduling_rules";
create trigger t_audit after insert or update or delete on aip."pno_scheduling_rules" for each row execute function aip.audit_row();
drop trigger if exists t_touch on aip."pno_scheduling_rules";
create trigger t_touch before update on aip."pno_scheduling_rules" for each row execute function aip.touch_updated_at();

-- PNO_Optimization_Objectives · Planning & Optimization · business key: Objective_ID
create table if not exists aip."pno_optimization_objectives" (
  tenant_id uuid not null references aip.tenants(id) on delete cascade,
  row_key text not null,
  "objective_id" varchar(200),
  "objective_name" varchar(200),
  "metric" varchar(200),
  "direction" varchar(200),
  "weight" integer,
  "governance_status" varchar(200),
  "selection_allowed" varchar(200),
  "default_selected" varchar(200),
  "ui_order" integer,
  created_at timestamptz not null default now(),
  updated_at timestamptz not null default now(),
  primary key (tenant_id, row_key)
);
comment on table aip."pno_optimization_objectives" is 'Workbook sheet "PNO_Optimization_Objectives"';
alter table aip."pno_optimization_objectives" enable row level security;
drop policy if exists p_read on aip."pno_optimization_objectives";
create policy p_read on aip."pno_optimization_objectives" for select to authenticated using (aip.has_role(tenant_id));
drop policy if exists p_write on aip."pno_optimization_objectives";
create policy p_write on aip."pno_optimization_objectives" for all to authenticated
  using (aip.has_role(tenant_id, array['planner','admin'])) with check (aip.has_role(tenant_id, array['planner','admin']));
drop trigger if exists t_audit on aip."pno_optimization_objectives";
create trigger t_audit after insert or update or delete on aip."pno_optimization_objectives" for each row execute function aip.audit_row();
drop trigger if exists t_touch on aip."pno_optimization_objectives";
create trigger t_touch before update on aip."pno_optimization_objectives" for each row execute function aip.touch_updated_at();

-- PNO_Optimization_Interface · Planning & Optimization · business key: Interface_ID
create table if not exists aip."pno_optimization_interface" (
  tenant_id uuid not null references aip.tenants(id) on delete cascade,
  row_key text not null,
  "interface_id" varchar(200),
  "engine" varchar(200),
  "direction" varchar(200),
  "payload" varchar(200),
  "status" varchar(200),
  "validation_required" varchar(200),
  created_at timestamptz not null default now(),
  updated_at timestamptz not null default now(),
  primary key (tenant_id, row_key)
);
comment on table aip."pno_optimization_interface" is 'Workbook sheet "PNO_Optimization_Interface"';
alter table aip."pno_optimization_interface" enable row level security;
drop policy if exists p_read on aip."pno_optimization_interface";
create policy p_read on aip."pno_optimization_interface" for select to authenticated using (aip.has_role(tenant_id));
drop policy if exists p_write on aip."pno_optimization_interface";
create policy p_write on aip."pno_optimization_interface" for all to authenticated
  using (aip.has_role(tenant_id, array['planner','admin'])) with check (aip.has_role(tenant_id, array['planner','admin']));
drop trigger if exists t_audit on aip."pno_optimization_interface";
create trigger t_audit after insert or update or delete on aip."pno_optimization_interface" for each row execute function aip.audit_row();
drop trigger if exists t_touch on aip."pno_optimization_interface";
create trigger t_touch before update on aip."pno_optimization_interface" for each row execute function aip.touch_updated_at();

-- PNO_Plan_Status_Config · Planning & Optimization · business key: Status_ID
create table if not exists aip."pno_plan_status_config" (
  tenant_id uuid not null references aip.tenants(id) on delete cascade,
  row_key text not null,
  "status_id" varchar(200),
  "plan_status" varchar(200),
  "plan_type" varchar(200),
  "governance_meaning" varchar(200),
  "locked" varchar(200),
  created_at timestamptz not null default now(),
  updated_at timestamptz not null default now(),
  primary key (tenant_id, row_key)
);
comment on table aip."pno_plan_status_config" is 'Workbook sheet "PNO_Plan_Status_Config"';
alter table aip."pno_plan_status_config" enable row level security;
drop policy if exists p_read on aip."pno_plan_status_config";
create policy p_read on aip."pno_plan_status_config" for select to authenticated using (aip.has_role(tenant_id));
drop policy if exists p_write on aip."pno_plan_status_config";
create policy p_write on aip."pno_plan_status_config" for all to authenticated
  using (aip.has_role(tenant_id, array['planner','admin'])) with check (aip.has_role(tenant_id, array['planner','admin']));
drop trigger if exists t_audit on aip."pno_plan_status_config";
create trigger t_audit after insert or update or delete on aip."pno_plan_status_config" for each row execute function aip.audit_row();
drop trigger if exists t_touch on aip."pno_plan_status_config";
create trigger t_touch before update on aip."pno_plan_status_config" for each row execute function aip.touch_updated_at();

-- PNO_Technician_Skills · Planning & Optimization · business key: Technician_ID
create table if not exists aip."pno_technician_skills" (
  tenant_id uuid not null references aip.tenants(id) on delete cascade,
  row_key text not null,
  "technician_id" varchar(200),
  "technician_name" varchar(200),
  "crew_id" varchar(200),
  "home_site" varchar(200),
  "role" varchar(200),
  "primary_skill" varchar(200),
  "skill_level" varchar(200),
  "certification_or_control" varchar(200),
  "valid_to" date,
  "standard_hours_day" integer,
  "regular_rate_hr" numeric(24,2),
  "overtime_rate_hr" numeric(24,2),
  "holiday_rate_hr" numeric(24,2),
  "productivity_factor" numeric(24,2),
  "basis" varchar(200),
  "source_url" text,
  "default_shift" varchar(200),
  "work_days" varchar(200),
  "ot_eligible" varchar(200),
  "max_ot_hours_day" integer,
  "max_ot_hours_week" integer,
  "holiday_work_eligible" varchar(200),
  "travel_eligible" varchar(200),
  "availability_status" varchar(200),
  currency char(3) not null default 'INR' check (currency ~ '^[A-Z]{3}$'),
  created_at timestamptz not null default now(),
  updated_at timestamptz not null default now(),
  primary key (tenant_id, row_key)
);
comment on table aip."pno_technician_skills" is 'Workbook sheet "PNO_Technician_Skills"';
alter table aip."pno_technician_skills" enable row level security;
drop policy if exists p_read on aip."pno_technician_skills";
create policy p_read on aip."pno_technician_skills" for select to authenticated using (aip.has_role(tenant_id));
drop policy if exists p_write on aip."pno_technician_skills";
create policy p_write on aip."pno_technician_skills" for all to authenticated
  using (aip.has_role(tenant_id, array['planner','admin'])) with check (aip.has_role(tenant_id, array['planner','admin']));
drop trigger if exists t_audit on aip."pno_technician_skills";
create trigger t_audit after insert or update or delete on aip."pno_technician_skills" for each row execute function aip.audit_row();
drop trigger if exists t_touch on aip."pno_technician_skills";
create trigger t_touch before update on aip."pno_technician_skills" for each row execute function aip.touch_updated_at();

-- PNO_Crew_Rates · Planning & Optimization · business key: Crew_ID
create table if not exists aip."pno_crew_rates" (
  tenant_id uuid not null references aip.tenants(id) on delete cascade,
  row_key text not null,
  "crew_id" varchar(200),
  "crew_size" integer,
  "avg_tech_rate_hr" numeric(24,2),
  "crew_labour_rate_hr" numeric(24,2),
  "mobilization_fixed" numeric(24,2),
  "daily_allowance" numeric(24,2),
  "standard_hours_day" integer,
  "ot_multiplier" numeric(24,2),
  "holiday_multiplier" numeric(24,2),
  "basis" varchar(200),
  "source_url" text,
  "night_shift_multiplier" numeric(24,2),
  "weekly_hours_cap" integer,
  "ot_approval_required" varchar(200),
  "holiday_work_approval" varchar(200),
  "rate_effective_from" date,
  currency char(3) not null default 'INR' check (currency ~ '^[A-Z]{3}$'),
  created_at timestamptz not null default now(),
  updated_at timestamptz not null default now(),
  primary key (tenant_id, row_key)
);
comment on table aip."pno_crew_rates" is 'Workbook sheet "PNO_Crew_Rates"';
alter table aip."pno_crew_rates" enable row level security;
drop policy if exists p_read on aip."pno_crew_rates";
create policy p_read on aip."pno_crew_rates" for select to authenticated using (aip.has_role(tenant_id));
drop policy if exists p_write on aip."pno_crew_rates";
create policy p_write on aip."pno_crew_rates" for all to authenticated
  using (aip.has_role(tenant_id, array['planner','admin'])) with check (aip.has_role(tenant_id, array['planner','admin']));
drop trigger if exists t_audit on aip."pno_crew_rates";
create trigger t_audit after insert or update or delete on aip."pno_crew_rates" for each row execute function aip.audit_row();
drop trigger if exists t_touch on aip."pno_crew_rates";
create trigger t_touch before update on aip."pno_crew_rates" for each row execute function aip.touch_updated_at();

-- PNO_Equipment_Rates · Planning & Optimization · business key: Equipment_ID
create table if not exists aip."pno_equipment_rates" (
  tenant_id uuid not null references aip.tenants(id) on delete cascade,
  row_key text not null,
  "equipment_id" varchar(200),
  "equipment_name" varchar(200),
  "type" varchar(200),
  "daily_rate" numeric(24,2),
  "hourly_rate" numeric(24,2),
  "mobilization" numeric(24,2),
  "minimum_days" integer,
  "standard_hours" integer,
  "control" varchar(200),
  "availability" varchar(200),
  "basis" varchar(200),
  "source_url" text,
  "reservation_lead_time_hours" integer,
  "ot_use_allowed" varchar(200),
  "availability_note" varchar(200),
  currency char(3) not null default 'INR' check (currency ~ '^[A-Z]{3}$'),
  created_at timestamptz not null default now(),
  updated_at timestamptz not null default now(),
  primary key (tenant_id, row_key)
);
comment on table aip."pno_equipment_rates" is 'Workbook sheet "PNO_Equipment_Rates"';
alter table aip."pno_equipment_rates" enable row level security;
drop policy if exists p_read on aip."pno_equipment_rates";
create policy p_read on aip."pno_equipment_rates" for select to authenticated using (aip.has_role(tenant_id));
drop policy if exists p_write on aip."pno_equipment_rates";
create policy p_write on aip."pno_equipment_rates" for all to authenticated
  using (aip.has_role(tenant_id, array['planner','admin'])) with check (aip.has_role(tenant_id, array['planner','admin']));
drop trigger if exists t_audit on aip."pno_equipment_rates";
create trigger t_audit after insert or update or delete on aip."pno_equipment_rates" for each row execute function aip.audit_row();
drop trigger if exists t_touch on aip."pno_equipment_rates";
create trigger t_touch before update on aip."pno_equipment_rates" for each row execute function aip.touch_updated_at();

-- PNO_Vehicle_Mobilization · Planning & Optimization · business key: Vehicle_Class_ID
create table if not exists aip."pno_vehicle_mobilization" (
  tenant_id uuid not null references aip.tenants(id) on delete cascade,
  row_key text not null,
  "vehicle_class_id" varchar(200),
  "vehicle_class" varchar(200),
  "variable_km" numeric(24,2),
  "daily_rate" numeric(24,2),
  "driver_hr" numeric(24,2),
  "mobilization_fixed" numeric(24,2),
  "overnight_driver_allowance" numeric(24,2),
  "included_km_day" integer,
  "crew_capacity" integer,
  "driver_ot_multiplier" numeric(24,2),
  "driver_holiday_multiplier" numeric(24,2),
  "source_basis" varchar(200),
  currency char(3) not null default 'INR' check (currency ~ '^[A-Z]{3}$'),
  created_at timestamptz not null default now(),
  updated_at timestamptz not null default now(),
  primary key (tenant_id, row_key)
);
comment on table aip."pno_vehicle_mobilization" is 'Workbook sheet "PNO_Vehicle_Mobilization"';
alter table aip."pno_vehicle_mobilization" enable row level security;
drop policy if exists p_read on aip."pno_vehicle_mobilization";
create policy p_read on aip."pno_vehicle_mobilization" for select to authenticated using (aip.has_role(tenant_id));
drop policy if exists p_write on aip."pno_vehicle_mobilization";
create policy p_write on aip."pno_vehicle_mobilization" for all to authenticated
  using (aip.has_role(tenant_id, array['planner','admin'])) with check (aip.has_role(tenant_id, array['planner','admin']));
drop trigger if exists t_audit on aip."pno_vehicle_mobilization";
create trigger t_audit after insert or update or delete on aip."pno_vehicle_mobilization" for each row execute function aip.audit_row();
drop trigger if exists t_touch on aip."pno_vehicle_mobilization";
create trigger t_touch before update on aip."pno_vehicle_mobilization" for each row execute function aip.touch_updated_at();

-- PNO_Material_Cost_Logistics · Planning & Optimization · business key: Material_ID
create table if not exists aip."pno_material_cost_logistics" (
  tenant_id uuid not null references aip.tenants(id) on delete cascade,
  row_key text not null,
  "material_id" varchar(200),
  "material_description" varchar(200),
  "unit_cost" numeric(24,2),
  "standard_freight" numeric(24,2),
  "inter_site_transfer" numeric(24,2),
  "emergency_expedite_premium" numeric(24,2),
  "receiving_handling" numeric(24,2),
  "lead_time_days" integer,
  "preferred_supplier" varchar(200),
  "storage_location" varchar(200),
  "optimization_treatment" varchar(200),
  "basis" varchar(200),
  "source_url" text,
  currency char(3) not null default 'INR' check (currency ~ '^[A-Z]{3}$'),
  created_at timestamptz not null default now(),
  updated_at timestamptz not null default now(),
  primary key (tenant_id, row_key)
);
comment on table aip."pno_material_cost_logistics" is 'Workbook sheet "PNO_Material_Cost_Logistics"';
alter table aip."pno_material_cost_logistics" enable row level security;
drop policy if exists p_read on aip."pno_material_cost_logistics";
create policy p_read on aip."pno_material_cost_logistics" for select to authenticated using (aip.has_role(tenant_id));
drop policy if exists p_write on aip."pno_material_cost_logistics";
create policy p_write on aip."pno_material_cost_logistics" for all to authenticated
  using (aip.has_role(tenant_id, array['planner','admin'])) with check (aip.has_role(tenant_id, array['planner','admin']));
drop trigger if exists t_audit on aip."pno_material_cost_logistics";
create trigger t_audit after insert or update or delete on aip."pno_material_cost_logistics" for each row execute function aip.audit_row();
drop trigger if exists t_touch on aip."pno_material_cost_logistics";
create trigger t_touch before update on aip."pno_material_cost_logistics" for each row execute function aip.touch_updated_at();

-- PNO_External_Service_Rates · Planning & Optimization · business key: Service_ID
create table if not exists aip."pno_external_service_rates" (
  tenant_id uuid not null references aip.tenants(id) on delete cascade,
  row_key text not null,
  "service_id" varchar(200),
  "service_name" varchar(200),
  "day_rate" numeric(24,2),
  "weekly_rate" numeric(24,2),
  "callout" numeric(24,2),
  "travel_allowance_day" numeric(24,2),
  "minimum_days" integer,
  "required_competency" varchar(200),
  currency char(3) not null default 'INR' check (currency ~ '^[A-Z]{3}$'),
  created_at timestamptz not null default now(),
  updated_at timestamptz not null default now(),
  primary key (tenant_id, row_key)
);
comment on table aip."pno_external_service_rates" is 'Workbook sheet "PNO_External_Service_Rates"';
alter table aip."pno_external_service_rates" enable row level security;
drop policy if exists p_read on aip."pno_external_service_rates";
create policy p_read on aip."pno_external_service_rates" for select to authenticated using (aip.has_role(tenant_id));
drop policy if exists p_write on aip."pno_external_service_rates";
create policy p_write on aip."pno_external_service_rates" for all to authenticated
  using (aip.has_role(tenant_id, array['planner','admin'])) with check (aip.has_role(tenant_id, array['planner','admin']));
drop trigger if exists t_audit on aip."pno_external_service_rates";
create trigger t_audit after insert or update or delete on aip."pno_external_service_rates" for each row execute function aip.audit_row();
drop trigger if exists t_touch on aip."pno_external_service_rates";
create trigger t_touch before update on aip."pno_external_service_rates" for each row execute function aip.touch_updated_at();

-- PNO_Site_Mobilization · Planning & Optimization · business key: Plant_ID
create table if not exists aip."pno_site_mobilization" (
  tenant_id uuid not null references aip.tenants(id) on delete cascade,
  row_key text not null,
  "plant_id" varchar(200),
  "state" varchar(200),
  "reference_roundtrip_km" integer,
  "vehicle_travel_cost" numeric(24,2),
  "lodging_person_night" numeric(24,2),
  "per_diem_person_day" numeric(24,2),
  "local_transport_person_day" numeric(24,2),
  "standard_shift_hours" integer,
  "normal_work_window" varchar(200),
  "governance_note" varchar(200),
  currency char(3) not null default 'INR' check (currency ~ '^[A-Z]{3}$'),
  created_at timestamptz not null default now(),
  updated_at timestamptz not null default now(),
  primary key (tenant_id, row_key)
);
comment on table aip."pno_site_mobilization" is 'Workbook sheet "PNO_Site_Mobilization"';
alter table aip."pno_site_mobilization" enable row level security;
drop policy if exists p_read on aip."pno_site_mobilization";
create policy p_read on aip."pno_site_mobilization" for select to authenticated using (aip.has_role(tenant_id));
drop policy if exists p_write on aip."pno_site_mobilization";
create policy p_write on aip."pno_site_mobilization" for all to authenticated
  using (aip.has_role(tenant_id, array['planner','admin'])) with check (aip.has_role(tenant_id, array['planner','admin']));
drop trigger if exists t_audit on aip."pno_site_mobilization";
create trigger t_audit after insert or update or delete on aip."pno_site_mobilization" for each row execute function aip.audit_row();
drop trigger if exists t_touch on aip."pno_site_mobilization";
create trigger t_touch before update on aip."pno_site_mobilization" for each row execute function aip.touch_updated_at();

-- PNO_Optimization_Cost_Config · Planning & Optimization · business key: Config_ID
create table if not exists aip."pno_optimization_cost_config" (
  tenant_id uuid not null references aip.tenants(id) on delete cascade,
  row_key text not null,
  "config_id" varchar(200),
  "parameter" varchar(200),
  "value_or_formula" varchar(200),
  "unit_or_type" varchar(200),
  "governance_status" varchar(200),
  "governance_note" varchar(200),
  created_at timestamptz not null default now(),
  updated_at timestamptz not null default now(),
  primary key (tenant_id, row_key)
);
comment on table aip."pno_optimization_cost_config" is 'Workbook sheet "PNO_Optimization_Cost_Config"';
alter table aip."pno_optimization_cost_config" enable row level security;
drop policy if exists p_read on aip."pno_optimization_cost_config";
create policy p_read on aip."pno_optimization_cost_config" for select to authenticated using (aip.has_role(tenant_id));
drop policy if exists p_write on aip."pno_optimization_cost_config";
create policy p_write on aip."pno_optimization_cost_config" for all to authenticated
  using (aip.has_role(tenant_id, array['planner','admin'])) with check (aip.has_role(tenant_id, array['planner','admin']));
drop trigger if exists t_audit on aip."pno_optimization_cost_config";
create trigger t_audit after insert or update or delete on aip."pno_optimization_cost_config" for each row execute function aip.audit_row();
drop trigger if exists t_touch on aip."pno_optimization_cost_config";
create trigger t_touch before update on aip."pno_optimization_cost_config" for each row execute function aip.touch_updated_at();

-- PNO_Intervention_Cost_Basis · Planning & Optimization · business key: Intervention_ID
create table if not exists aip."pno_intervention_cost_basis" (
  tenant_id uuid not null references aip.tenants(id) on delete cascade,
  row_key text not null,
  "intervention_id" varchar(200),
  "work_order_id" varchar(200),
  "plant_id" varchar(200),
  "asset_id" varchar(200),
  "asset_class" varchar(200),
  "required_skill" varchar(200),
  "duration_hours" integer,
  "crew_qty" integer,
  "labour_cost" numeric(24,2),
  "mobilization_cost" numeric(24,2),
  "vehicle_cost" numeric(24,2),
  "equipment_cost" numeric(24,2),
  "material_cost" numeric(24,2),
  "logistics_cost" numeric(24,2),
  "external_service_cost" numeric(24,2),
  "permit_external_fee" numeric(24,2),
  "total_execution_cost" numeric(24,2),
  "calculation_basis" varchar(200),
  currency char(3) not null default 'INR' check (currency ~ '^[A-Z]{3}$'),
  created_at timestamptz not null default now(),
  updated_at timestamptz not null default now(),
  primary key (tenant_id, row_key)
);
comment on table aip."pno_intervention_cost_basis" is 'Workbook sheet "PNO_Intervention_Cost_Basis"';
alter table aip."pno_intervention_cost_basis" enable row level security;
drop policy if exists p_read on aip."pno_intervention_cost_basis";
create policy p_read on aip."pno_intervention_cost_basis" for select to authenticated using (aip.has_role(tenant_id));
drop policy if exists p_write on aip."pno_intervention_cost_basis";
create policy p_write on aip."pno_intervention_cost_basis" for all to authenticated
  using (aip.has_role(tenant_id, array['planner','admin'])) with check (aip.has_role(tenant_id, array['planner','admin']));
drop trigger if exists t_audit on aip."pno_intervention_cost_basis";
create trigger t_audit after insert or update or delete on aip."pno_intervention_cost_basis" for each row execute function aip.audit_row();
drop trigger if exists t_touch on aip."pno_intervention_cost_basis";
create trigger t_touch before update on aip."pno_intervention_cost_basis" for each row execute function aip.touch_updated_at();

-- PNO_Optimization_Source_Reg · Planning & Optimization · business key: Source_ID
create table if not exists aip."pno_optimization_source_reg" (
  tenant_id uuid not null references aip.tenants(id) on delete cascade,
  row_key text not null,
  "source_id" varchar(200),
  "source" varchar(200),
  "evidence" varchar(200),
  "use_in_model" varchar(200),
  "source_url" text,
  created_at timestamptz not null default now(),
  updated_at timestamptz not null default now(),
  primary key (tenant_id, row_key)
);
comment on table aip."pno_optimization_source_reg" is 'Workbook sheet "PNO_Optimization_Source_Reg"';
alter table aip."pno_optimization_source_reg" enable row level security;
drop policy if exists p_read on aip."pno_optimization_source_reg";
create policy p_read on aip."pno_optimization_source_reg" for select to authenticated using (aip.has_role(tenant_id));
drop policy if exists p_write on aip."pno_optimization_source_reg";
create policy p_write on aip."pno_optimization_source_reg" for all to authenticated
  using (aip.has_role(tenant_id, array['planner','admin'])) with check (aip.has_role(tenant_id, array['planner','admin']));
drop trigger if exists t_audit on aip."pno_optimization_source_reg";
create trigger t_audit after insert or update or delete on aip."pno_optimization_source_reg" for each row execute function aip.audit_row();
drop trigger if exists t_touch on aip."pno_optimization_source_reg";
create trigger t_touch before update on aip."pno_optimization_source_reg" for each row execute function aip.touch_updated_at();

-- VE_Exposure_Summary · Value Exposure · business key: Intervention_ID
create table if not exists aip."ve_exposure_summary" (
  tenant_id uuid not null references aip.tenants(id) on delete cascade,
  row_key text not null,
  "intervention_id" varchar(200),
  "plant_id" varchar(200),
  "asset_tag" varchar(200),
  "intervention" varchar(200),
  "calculation_as_of" varchar(200),
  "required_by" varchar(200),
  "planned_start" varchar(200),
  "value_exposure_as_of" numeric(24,2),
  "ve_as_of_plus_7_d" numeric(24,2),
  "ve_as_of_plus_14_d" numeric(24,2),
  "ve_as_of_plus_21_d" numeric(24,2),
  "ve_at_required_by" numeric(24,2),
  "ve_required_by_plus_7_d" numeric(24,2),
  "ve_required_by_plus_30_d" numeric(24,2),
  "ve_at_planned_start" numeric(24,2),
  "cod_as_of_to_plus7_d" numeric(24,2),
  "cod_as_of_to_plus14_d" numeric(24,2),
  "cod_as_of_to_required_by" numeric(24,2),
  "cod_required_by_to_plus7_d" numeric(24,2),
  "model_version" varchar(200),
  "calculation_status" varchar(200),
  "data_parity" varchar(200),
  "formula_check_current" numeric(24,4),
  "recalc_cod_as_of_to_plus7_d" numeric(24,2),
  "cod_7_d_difference" numeric(24,2),
  "cod_7_d_check_status" varchar(200),
  "current_exposure_check_status" varchar(200),
  "lineage_basis" varchar(200),
  "audit_readiness" varchar(200),
  currency char(3) not null default 'INR' check (currency ~ '^[A-Z]{3}$'),
  created_at timestamptz not null default now(),
  updated_at timestamptz not null default now(),
  primary key (tenant_id, row_key)
);
comment on table aip."ve_exposure_summary" is 'Workbook sheet "VE_Exposure_Summary"';
alter table aip."ve_exposure_summary" enable row level security;
drop policy if exists p_read on aip."ve_exposure_summary";
create policy p_read on aip."ve_exposure_summary" for select to authenticated using (aip.has_role(tenant_id));
drop policy if exists p_write on aip."ve_exposure_summary";
create policy p_write on aip."ve_exposure_summary" for all to authenticated
  using (aip.has_role(tenant_id, array['planner','admin'])) with check (aip.has_role(tenant_id, array['planner','admin']));
drop trigger if exists t_audit on aip."ve_exposure_summary";
create trigger t_audit after insert or update or delete on aip."ve_exposure_summary" for each row execute function aip.audit_row();
drop trigger if exists t_touch on aip."ve_exposure_summary";
create trigger t_touch before update on aip."ve_exposure_summary" for each row execute function aip.touch_updated_at();

-- PNO_Data_Window_Config · Planning & Optimization · business key: Config_ID
create table if not exists aip."pno_data_window_config" (
  tenant_id uuid not null references aip.tenants(id) on delete cascade,
  row_key text not null,
  "config_id" varchar(200),
  "reference_date" timestamp without time zone,
  "available_from" varchar(200),
  "available_to" varchar(200),
  "lookback_days" integer,
  "forward_months" integer,
  "rebase_mode" varchar(200),
  "custom_range_default_from" varchar(200),
  "custom_range_default_to" varchar(200),
  "governance_status" varchar(200),
  created_at timestamptz not null default now(),
  updated_at timestamptz not null default now(),
  primary key (tenant_id, row_key)
);
comment on table aip."pno_data_window_config" is 'Workbook sheet "PNO_Data_Window_Config"';
alter table aip."pno_data_window_config" enable row level security;
drop policy if exists p_read on aip."pno_data_window_config";
create policy p_read on aip."pno_data_window_config" for select to authenticated using (aip.has_role(tenant_id));
drop policy if exists p_write on aip."pno_data_window_config";
create policy p_write on aip."pno_data_window_config" for all to authenticated
  using (aip.has_role(tenant_id, array['planner','admin'])) with check (aip.has_role(tenant_id, array['planner','admin']));
drop trigger if exists t_audit on aip."pno_data_window_config";
create trigger t_audit after insert or update or delete on aip."pno_data_window_config" for each row execute function aip.audit_row();
drop trigger if exists t_touch on aip."pno_data_window_config";
create trigger t_touch before update on aip."pno_data_window_config" for each row execute function aip.touch_updated_at();

-- PLAN_Interventions · Planning & Optimization · business key: Intervention_ID
create table if not exists aip."plan_interventions" (
  tenant_id uuid not null references aip.tenants(id) on delete cascade,
  row_key text not null,
  "intervention_id" varchar(200),
  "work_order_id" varchar(200),
  "plant_id" varchar(200),
  "asset_id" varchar(200),
  "asset_tag" varchar(200),
  "asset_class" varchar(200),
  "maintenance_type" varchar(200),
  "source_record_id" varchar(200),
  "source_sheet" varchar(200),
  "intervention" varchar(200),
  "priority" varchar(200),
  "risk_score_pct" numeric(24,2),
  "value_exposure" numeric(24,2),
  "required_by" timestamp without time zone,
  "planned_start" timestamp without time zone,
  "duration_hours" integer,
  "required_skill" varchar(200),
  "required_crew_qty" integer,
  "required_part_id" varchar(200),
  "required_part_qty" integer,
  "required_tool_id" varchar(200),
  "vehicle_class" varchar(200),
  "permit_required" varchar(200),
  "outage_required" varchar(200),
  "planning_status" varchar(200),
  "planning_as_of" timestamp without time zone,
  "calculation_basis" varchar(1000),
  "planned_finish" timestamp without time zone,
  "actual_start" timestamp without time zone,
  "actual_duration_hours" integer,
  "actual_completion" timestamp without time zone,
  "erp_execution_status" varchar(200),
  "actual_source" varchar(200),
  currency char(3) not null default 'INR' check (currency ~ '^[A-Z]{3}$'),
  created_at timestamptz not null default now(),
  updated_at timestamptz not null default now(),
  primary key (tenant_id, row_key)
);
comment on table aip."plan_interventions" is 'Workbook sheet "PLAN_Interventions"';
alter table aip."plan_interventions" enable row level security;
drop policy if exists p_read on aip."plan_interventions";
create policy p_read on aip."plan_interventions" for select to authenticated using (aip.has_role(tenant_id));
drop policy if exists p_write on aip."plan_interventions";
create policy p_write on aip."plan_interventions" for all to authenticated
  using (aip.has_role(tenant_id, array['planner','admin'])) with check (aip.has_role(tenant_id, array['planner','admin']));
drop trigger if exists t_audit on aip."plan_interventions";
create trigger t_audit after insert or update or delete on aip."plan_interventions" for each row execute function aip.audit_row();
drop trigger if exists t_touch on aip."plan_interventions";
create trigger t_touch before update on aip."plan_interventions" for each row execute function aip.touch_updated_at();

-- PLAN_Requirements · Planning & Optimization · business key: Requirement_ID
create table if not exists aip."plan_requirements" (
  tenant_id uuid not null references aip.tenants(id) on delete cascade,
  row_key text not null,
  "requirement_id" varchar(200),
  "intervention_id" varchar(200),
  "requirement_type" varchar(200),
  "requirement_ref" varchar(200),
  "quantity" integer,
  "mandatory" varchar(200),
  "source" varchar(200),
  created_at timestamptz not null default now(),
  updated_at timestamptz not null default now(),
  primary key (tenant_id, row_key)
);
comment on table aip."plan_requirements" is 'Workbook sheet "PLAN_Requirements"';
alter table aip."plan_requirements" enable row level security;
drop policy if exists p_read on aip."plan_requirements";
create policy p_read on aip."plan_requirements" for select to authenticated using (aip.has_role(tenant_id));
drop policy if exists p_write on aip."plan_requirements";
create policy p_write on aip."plan_requirements" for all to authenticated
  using (aip.has_role(tenant_id, array['planner','admin'])) with check (aip.has_role(tenant_id, array['planner','admin']));
drop trigger if exists t_audit on aip."plan_requirements";
create trigger t_audit after insert or update or delete on aip."plan_requirements" for each row execute function aip.audit_row();
drop trigger if exists t_touch on aip."plan_requirements";
create trigger t_touch before update on aip."plan_requirements" for each row execute function aip.touch_updated_at();

-- PLAN_Readiness · Planning & Optimization · business key: Intervention_ID
create table if not exists aip."plan_readiness" (
  tenant_id uuid not null references aip.tenants(id) on delete cascade,
  row_key text not null,
  "intervention_id" varchar(200),
  "crew_status" varchar(200),
  "material_status" varchar(200),
  "tool_status" varchar(200),
  "vehicle_status" varchar(200),
  "access_status" varchar(200),
  "outage_status" varchar(200),
  "overall_readiness_pct" numeric(24,2),
  "overall_status" varchar(200),
  "primary_constraint" varchar(200),
  "material_on_hand_qty" integer,
  "material_earliest_receipt" varchar(200),
  "source" varchar(200),
  "last_updated" timestamp without time zone,
  created_at timestamptz not null default now(),
  updated_at timestamptz not null default now(),
  primary key (tenant_id, row_key)
);
comment on table aip."plan_readiness" is 'Workbook sheet "PLAN_Readiness"';
alter table aip."plan_readiness" enable row level security;
drop policy if exists p_read on aip."plan_readiness";
create policy p_read on aip."plan_readiness" for select to authenticated using (aip.has_role(tenant_id));
drop policy if exists p_write on aip."plan_readiness";
create policy p_write on aip."plan_readiness" for all to authenticated
  using (aip.has_role(tenant_id, array['planner','admin'])) with check (aip.has_role(tenant_id, array['planner','admin']));
drop trigger if exists t_audit on aip."plan_readiness";
create trigger t_audit after insert or update or delete on aip."plan_readiness" for each row execute function aip.audit_row();
drop trigger if exists t_touch on aip."plan_readiness";
create trigger t_touch before update on aip."plan_readiness" for each row execute function aip.touch_updated_at();

-- PLAN_Schedule · Planning & Optimization · business key: Schedule_ID
create table if not exists aip."plan_schedule" (
  tenant_id uuid not null references aip.tenants(id) on delete cascade,
  row_key text not null,
  "schedule_id" varchar(200),
  "intervention_id" varchar(200),
  "plan_type" varchar(200),
  "planned_start" timestamp without time zone,
  "planned_end" timestamp without time zone,
  "planned_finish" timestamp without time zone,
  "duration_hours" integer,
  "crew_id" varchar(200),
  "technician_id" varchar(200),
  "vehicle_id" varchar(200),
  "scenario_id" varchar(200),
  "status" varchar(200),
  "source" varchar(200),
  "aip_optimized_start" timestamp without time zone,
  "aip_optimized_duration_hours" integer,
  "aip_optimized_finish" timestamp without time zone,
  "approved_start" timestamp without time zone,
  "approved_duration_hours" integer,
  "approved_finish" timestamp without time zone,
  "schedule_path" varchar(200),
  "actual_start" timestamp without time zone,
  "actual_duration_hours" integer,
  "actual_completion" timestamp without time zone,
  "erp_execution_status" varchar(200),
  "actual_source" varchar(200),
  created_at timestamptz not null default now(),
  updated_at timestamptz not null default now(),
  primary key (tenant_id, row_key)
);
comment on table aip."plan_schedule" is 'Workbook sheet "PLAN_Schedule"';
alter table aip."plan_schedule" enable row level security;
drop policy if exists p_read on aip."plan_schedule";
create policy p_read on aip."plan_schedule" for select to authenticated using (aip.has_role(tenant_id));
drop policy if exists p_write on aip."plan_schedule";
create policy p_write on aip."plan_schedule" for all to authenticated
  using (aip.has_role(tenant_id, array['planner','admin'])) with check (aip.has_role(tenant_id, array['planner','admin']));
drop trigger if exists t_audit on aip."plan_schedule";
create trigger t_audit after insert or update or delete on aip."plan_schedule" for each row execute function aip.audit_row();
drop trigger if exists t_touch on aip."plan_schedule";
create trigger t_touch before update on aip."plan_schedule" for each row execute function aip.touch_updated_at();

-- PLAN_Governance_Handoff · Planning & Optimization · business key: Governance_ID
create table if not exists aip."plan_governance_handoff" (
  tenant_id uuid not null references aip.tenants(id) on delete cascade,
  row_key text not null,
  "governance_id" varchar(200),
  "intervention_id" varchar(200),
  "recommendation_status" varchar(200),
  "selection_status" varchar(200),
  "approval_status" varchar(200),
  "handoff_status" varchar(200),
  "erp_eam_acknowledgement" varchar(200),
  "governance_stage" varchar(200),
  "last_action" timestamp without time zone,
  "source" varchar(200),
  created_at timestamptz not null default now(),
  updated_at timestamptz not null default now(),
  primary key (tenant_id, row_key)
);
comment on table aip."plan_governance_handoff" is 'Workbook sheet "PLAN_Governance_Handoff"';
alter table aip."plan_governance_handoff" enable row level security;
drop policy if exists p_read on aip."plan_governance_handoff";
create policy p_read on aip."plan_governance_handoff" for select to authenticated using (aip.has_role(tenant_id));
drop policy if exists p_write on aip."plan_governance_handoff";
create policy p_write on aip."plan_governance_handoff" for all to authenticated
  using (aip.has_role(tenant_id, array['planner','admin'])) with check (aip.has_role(tenant_id, array['planner','admin']));
drop trigger if exists t_audit on aip."plan_governance_handoff";
create trigger t_audit after insert or update or delete on aip."plan_governance_handoff" for each row execute function aip.audit_row();
drop trigger if exists t_touch on aip."plan_governance_handoff";
create trigger t_touch before update on aip."plan_governance_handoff" for each row execute function aip.touch_updated_at();

-- PLAN_Optimization_Config · Planning & Optimization · business key: Config_ID
create table if not exists aip."plan_optimization_config" (
  tenant_id uuid not null references aip.tenants(id) on delete cascade,
  row_key text not null,
  "config_id" varchar(200),
  "parameter" varchar(200),
  "value" varchar(200),
  "unit" varchar(200),
  "priority" integer,
  "governance_basis" varchar(1000),
  created_at timestamptz not null default now(),
  updated_at timestamptz not null default now(),
  primary key (tenant_id, row_key)
);
comment on table aip."plan_optimization_config" is 'Workbook sheet "PLAN_Optimization_Config"';
alter table aip."plan_optimization_config" enable row level security;
drop policy if exists p_read on aip."plan_optimization_config";
create policy p_read on aip."plan_optimization_config" for select to authenticated using (aip.has_role(tenant_id));
drop policy if exists p_write on aip."plan_optimization_config";
create policy p_write on aip."plan_optimization_config" for all to authenticated
  using (aip.has_role(tenant_id, array['planner','admin'])) with check (aip.has_role(tenant_id, array['planner','admin']));
drop trigger if exists t_audit on aip."plan_optimization_config";
create trigger t_audit after insert or update or delete on aip."plan_optimization_config" for each row execute function aip.audit_row();
drop trigger if exists t_touch on aip."plan_optimization_config";
create trigger t_touch before update on aip."plan_optimization_config" for each row execute function aip.touch_updated_at();

-- PLAN_Attention_Rules · Planning & Optimization · business key: Rule_ID
create table if not exists aip."plan_attention_rules" (
  tenant_id uuid not null references aip.tenants(id) on delete cascade,
  row_key text not null,
  "rule_id" varchar(200),
  "rule_name" varchar(200),
  "rule_type" varchar(200),
  "threshold_value" integer,
  "threshold_unit" varchar(200),
  "evaluation_basis" varchar(200),
  "severity_weight" integer,
  "effective_date" timestamp without time zone,
  "governance_status" varchar(200),
  "description" varchar(1000),
  created_at timestamptz not null default now(),
  updated_at timestamptz not null default now(),
  primary key (tenant_id, row_key)
);
comment on table aip."plan_attention_rules" is 'Workbook sheet "PLAN_Attention_Rules"';
alter table aip."plan_attention_rules" enable row level security;
drop policy if exists p_read on aip."plan_attention_rules";
create policy p_read on aip."plan_attention_rules" for select to authenticated using (aip.has_role(tenant_id));
drop policy if exists p_write on aip."plan_attention_rules";
create policy p_write on aip."plan_attention_rules" for all to authenticated
  using (aip.has_role(tenant_id, array['planner','admin'])) with check (aip.has_role(tenant_id, array['planner','admin']));
drop trigger if exists t_audit on aip."plan_attention_rules";
create trigger t_audit after insert or update or delete on aip."plan_attention_rules" for each row execute function aip.audit_row();
drop trigger if exists t_touch on aip."plan_attention_rules";
create trigger t_touch before update on aip."plan_attention_rules" for each row execute function aip.touch_updated_at();

-- PLAN_Data_Window_Config · Planning & Optimization · business key: Config_ID
create table if not exists aip."plan_data_window_config" (
  tenant_id uuid not null references aip.tenants(id) on delete cascade,
  row_key text not null,
  "config_id" varchar(200),
  "reference_date" timestamp without time zone,
  "available_from" varchar(200),
  "available_to" varchar(200),
  "lookback_days" integer,
  "forward_months" integer,
  "rebase_mode" varchar(200),
  "custom_range_default_from" varchar(200),
  "custom_range_default_to" varchar(200),
  "governance_status" varchar(200),
  created_at timestamptz not null default now(),
  updated_at timestamptz not null default now(),
  primary key (tenant_id, row_key)
);
comment on table aip."plan_data_window_config" is 'Workbook sheet "PLAN_Data_Window_Config"';
alter table aip."plan_data_window_config" enable row level security;
drop policy if exists p_read on aip."plan_data_window_config";
create policy p_read on aip."plan_data_window_config" for select to authenticated using (aip.has_role(tenant_id));
drop policy if exists p_write on aip."plan_data_window_config";
create policy p_write on aip."plan_data_window_config" for all to authenticated
  using (aip.has_role(tenant_id, array['planner','admin'])) with check (aip.has_role(tenant_id, array['planner','admin']));
drop trigger if exists t_audit on aip."plan_data_window_config";
create trigger t_audit after insert or update or delete on aip."plan_data_window_config" for each row execute function aip.audit_row();
drop trigger if exists t_touch on aip."plan_data_window_config";
create trigger t_touch before update on aip."plan_data_window_config" for each row execute function aip.touch_updated_at();

-- PLAN_RCM_Context · Planning & Optimization · business key: RCM_Context_ID
create table if not exists aip."plan_rcm_context" (
  tenant_id uuid not null references aip.tenants(id) on delete cascade,
  row_key text not null,
  "rcm_context_id" varchar(200),
  "intervention_id" varchar(200),
  "plant_id" varchar(200),
  "asset_id" varchar(200),
  "failure_mode" varchar(200),
  "consequence_class" varchar(200),
  "criticality" varchar(200),
  "condition_score_pct" numeric(24,2),
  "rul_days" integer,
  "deferral_tolerance_days" integer,
  "rcm_policy" varchar(200),
  "source_module" varchar(200),
  "as_of" timestamp without time zone,
  created_at timestamptz not null default now(),
  updated_at timestamptz not null default now(),
  primary key (tenant_id, row_key)
);
comment on table aip."plan_rcm_context" is 'Workbook sheet "PLAN_RCM_Context"';
alter table aip."plan_rcm_context" enable row level security;
drop policy if exists p_read on aip."plan_rcm_context";
create policy p_read on aip."plan_rcm_context" for select to authenticated using (aip.has_role(tenant_id));
drop policy if exists p_write on aip."plan_rcm_context";
create policy p_write on aip."plan_rcm_context" for all to authenticated
  using (aip.has_role(tenant_id, array['planner','admin'])) with check (aip.has_role(tenant_id, array['planner','admin']));
drop trigger if exists t_audit on aip."plan_rcm_context";
create trigger t_audit after insert or update or delete on aip."plan_rcm_context" for each row execute function aip.audit_row();
drop trigger if exists t_touch on aip."plan_rcm_context";
create trigger t_touch before update on aip."plan_rcm_context" for each row execute function aip.touch_updated_at();

-- PLAN_Tool_Master · Planning & Optimization · business key: Tool_ID
create table if not exists aip."plan_tool_master" (
  tenant_id uuid not null references aip.tenants(id) on delete cascade,
  row_key text not null,
  "tool_id" varchar(200),
  "tool_name" varchar(200),
  "tool_type" varchar(200),
  "home_region" varchar(200),
  "calibration_status" varchar(200),
  "calibration_expiry" date,
  "availability" varchar(200),
  "applicable_asset_class" varchar(200),
  "control_requirement" varchar(200),
  "safety_category" varchar(200),
  "storage_location" varchar(200),
  "source" varchar(200),
  created_at timestamptz not null default now(),
  updated_at timestamptz not null default now(),
  primary key (tenant_id, row_key)
);
comment on table aip."plan_tool_master" is 'Workbook sheet "PLAN_Tool_Master"';
alter table aip."plan_tool_master" enable row level security;
drop policy if exists p_read on aip."plan_tool_master";
create policy p_read on aip."plan_tool_master" for select to authenticated using (aip.has_role(tenant_id));
drop policy if exists p_write on aip."plan_tool_master";
create policy p_write on aip."plan_tool_master" for all to authenticated
  using (aip.has_role(tenant_id, array['planner','admin'])) with check (aip.has_role(tenant_id, array['planner','admin']));
drop trigger if exists t_audit on aip."plan_tool_master";
create trigger t_audit after insert or update or delete on aip."plan_tool_master" for each row execute function aip.audit_row();
drop trigger if exists t_touch on aip."plan_tool_master";
create trigger t_touch before update on aip."plan_tool_master" for each row execute function aip.touch_updated_at();

-- PLAN_Vehicle_Master · Planning & Optimization · business key: Vehicle_ID
create table if not exists aip."plan_vehicle_master" (
  tenant_id uuid not null references aip.tenants(id) on delete cascade,
  row_key text not null,
  "vehicle_id" varchar(200),
  "vehicle_type" varchar(200),
  "home_plant_id" varchar(200),
  "fuel_type" varchar(200),
  "payload_kg" integer,
  "availability" varchar(200),
  "maintenance_status" varchar(200),
  "odometer_km" integer,
  "ghg_boundary_status" varchar(200),
  "source" varchar(200),
  "vehicle_use" varchar(200),
  "fuel_efficiency_km_per_l" numeric(24,2),
  "last_service_date" date,
  "next_service_due_km" integer,
  created_at timestamptz not null default now(),
  updated_at timestamptz not null default now(),
  primary key (tenant_id, row_key)
);
comment on table aip."plan_vehicle_master" is 'Workbook sheet "PLAN_Vehicle_Master"';
alter table aip."plan_vehicle_master" enable row level security;
drop policy if exists p_read on aip."plan_vehicle_master";
create policy p_read on aip."plan_vehicle_master" for select to authenticated using (aip.has_role(tenant_id));
drop policy if exists p_write on aip."plan_vehicle_master";
create policy p_write on aip."plan_vehicle_master" for all to authenticated
  using (aip.has_role(tenant_id, array['planner','admin'])) with check (aip.has_role(tenant_id, array['planner','admin']));
drop trigger if exists t_audit on aip."plan_vehicle_master";
create trigger t_audit after insert or update or delete on aip."plan_vehicle_master" for each row execute function aip.audit_row();
drop trigger if exists t_touch on aip."plan_vehicle_master";
create trigger t_touch before update on aip."plan_vehicle_master" for each row execute function aip.touch_updated_at();

-- PLAN_Capability_Config · Planning & Optimization · business key: Capability_ID
create table if not exists aip."plan_capability_config" (
  tenant_id uuid not null references aip.tenants(id) on delete cascade,
  row_key text not null,
  "capability_id" varchar(200),
  "capability" varchar(200),
  "ownership" varchar(200),
  "provider" varchar(200),
  "status" varchar(200),
  "configuration_source" varchar(200),
  "effective_date" date,
  created_at timestamptz not null default now(),
  updated_at timestamptz not null default now(),
  primary key (tenant_id, row_key)
);
comment on table aip."plan_capability_config" is 'Workbook sheet "PLAN_Capability_Config"';
alter table aip."plan_capability_config" enable row level security;
drop policy if exists p_read on aip."plan_capability_config";
create policy p_read on aip."plan_capability_config" for select to authenticated using (aip.has_role(tenant_id));
drop policy if exists p_write on aip."plan_capability_config";
create policy p_write on aip."plan_capability_config" for all to authenticated
  using (aip.has_role(tenant_id, array['planner','admin'])) with check (aip.has_role(tenant_id, array['planner','admin']));
drop trigger if exists t_audit on aip."plan_capability_config";
create trigger t_audit after insert or update or delete on aip."plan_capability_config" for each row execute function aip.audit_row();
drop trigger if exists t_touch on aip."plan_capability_config";
create trigger t_touch before update on aip."plan_capability_config" for each row execute function aip.touch_updated_at();

-- PLAN_System_Landscape · Planning & Optimization · business key: System_ID
create table if not exists aip."plan_system_landscape" (
  tenant_id uuid not null references aip.tenants(id) on delete cascade,
  row_key text not null,
  "system_id" varchar(200),
  "system_name" varchar(200),
  "system_type" varchar(200),
  "configuration_status" varchar(200),
  "connection_method" varchar(200),
  "environment" varchar(200),
  "source" varchar(200),
  created_at timestamptz not null default now(),
  updated_at timestamptz not null default now(),
  primary key (tenant_id, row_key)
);
comment on table aip."plan_system_landscape" is 'Workbook sheet "PLAN_System_Landscape"';
alter table aip."plan_system_landscape" enable row level security;
drop policy if exists p_read on aip."plan_system_landscape";
create policy p_read on aip."plan_system_landscape" for select to authenticated using (aip.has_role(tenant_id));
drop policy if exists p_write on aip."plan_system_landscape";
create policy p_write on aip."plan_system_landscape" for all to authenticated
  using (aip.has_role(tenant_id, array['planner','admin'])) with check (aip.has_role(tenant_id, array['planner','admin']));
drop trigger if exists t_audit on aip."plan_system_landscape";
create trigger t_audit after insert or update or delete on aip."plan_system_landscape" for each row execute function aip.audit_row();
drop trigger if exists t_touch on aip."plan_system_landscape";
create trigger t_touch before update on aip."plan_system_landscape" for each row execute function aip.touch_updated_at();

-- PLAN_Interface_Catalogue · Planning & Optimization · business key: Interface_ID
create table if not exists aip."plan_interface_catalogue" (
  tenant_id uuid not null references aip.tenants(id) on delete cascade,
  row_key text not null,
  "interface_id" varchar(200),
  "interface_name" varchar(200),
  "source_system" varchar(200),
  "target_system" varchar(200),
  "direction" varchar(200),
  "data_object" varchar(200),
  "frequency" varchar(200),
  "status" varchar(200),
  "last_validated" timestamp without time zone,
  "validation_rule" varchar(200),
  "source" varchar(200),
  created_at timestamptz not null default now(),
  updated_at timestamptz not null default now(),
  primary key (tenant_id, row_key)
);
comment on table aip."plan_interface_catalogue" is 'Workbook sheet "PLAN_Interface_Catalogue"';
alter table aip."plan_interface_catalogue" enable row level security;
drop policy if exists p_read on aip."plan_interface_catalogue";
create policy p_read on aip."plan_interface_catalogue" for select to authenticated using (aip.has_role(tenant_id));
drop policy if exists p_write on aip."plan_interface_catalogue";
create policy p_write on aip."plan_interface_catalogue" for all to authenticated
  using (aip.has_role(tenant_id, array['planner','admin'])) with check (aip.has_role(tenant_id, array['planner','admin']));
drop trigger if exists t_audit on aip."plan_interface_catalogue";
create trigger t_audit after insert or update or delete on aip."plan_interface_catalogue" for each row execute function aip.audit_row();
drop trigger if exists t_touch on aip."plan_interface_catalogue";
create trigger t_touch before update on aip."plan_interface_catalogue" for each row execute function aip.touch_updated_at();

-- PLAN_Calendar · Planning & Optimization · business key: Calendar_Event_ID
create table if not exists aip."plan_calendar" (
  tenant_id uuid not null references aip.tenants(id) on delete cascade,
  row_key text not null,
  "calendar_event_id" varchar(200),
  "calendar_level" varchar(200),
  "reference_id" varchar(200),
  "start" timestamp without time zone,
  "end" timestamp without time zone,
  "event_type" varchar(200),
  "availability" varchar(200),
  "capacity_pct" integer,
  "reason" varchar(200),
  "source" varchar(200),
  created_at timestamptz not null default now(),
  updated_at timestamptz not null default now(),
  primary key (tenant_id, row_key)
);
comment on table aip."plan_calendar" is 'Workbook sheet "PLAN_Calendar"';
alter table aip."plan_calendar" enable row level security;
drop policy if exists p_read on aip."plan_calendar";
create policy p_read on aip."plan_calendar" for select to authenticated using (aip.has_role(tenant_id));
drop policy if exists p_write on aip."plan_calendar";
create policy p_write on aip."plan_calendar" for all to authenticated
  using (aip.has_role(tenant_id, array['planner','admin'])) with check (aip.has_role(tenant_id, array['planner','admin']));
drop trigger if exists t_audit on aip."plan_calendar";
create trigger t_audit after insert or update or delete on aip."plan_calendar" for each row execute function aip.audit_row();
drop trigger if exists t_touch on aip."plan_calendar";
create trigger t_touch before update on aip."plan_calendar" for each row execute function aip.touch_updated_at();

-- PLAN_Weather_Connector_Config · Planning & Optimization · business key: Config_ID
create table if not exists aip."plan_weather_connector_config" (
  tenant_id uuid not null references aip.tenants(id) on delete cascade,
  row_key text not null,
  "config_id" varchar(200),
  "provider" varchar(200),
  "provider_role" varchar(200),
  "interface_type" varchar(200),
  "ingestion_mode" varchar(200),
  "refresh_cadence" varchar(200),
  "authentication" varchar(200),
  "payload_format" varchar(200),
  "transport" varchar(200),
  "environment" varchar(200),
  "priority" varchar(200),
  "fallback_provider" varchar(200),
  "activation" varchar(200),
  "aip_target" varchar(200),
  "endpoint_profile" varchar(200),
  created_at timestamptz not null default now(),
  updated_at timestamptz not null default now(),
  primary key (tenant_id, row_key)
);
comment on table aip."plan_weather_connector_config" is 'Workbook sheet "PLAN_Weather_Connector_Config"';
alter table aip."plan_weather_connector_config" enable row level security;
drop policy if exists p_read on aip."plan_weather_connector_config";
create policy p_read on aip."plan_weather_connector_config" for select to authenticated using (aip.has_role(tenant_id));
drop policy if exists p_write on aip."plan_weather_connector_config";
create policy p_write on aip."plan_weather_connector_config" for all to authenticated
  using (aip.has_role(tenant_id, array['planner','admin'])) with check (aip.has_role(tenant_id, array['planner','admin']));
drop trigger if exists t_audit on aip."plan_weather_connector_config";
create trigger t_audit after insert or update or delete on aip."plan_weather_connector_config" for each row execute function aip.audit_row();
drop trigger if exists t_touch on aip."plan_weather_connector_config";
create trigger t_touch before update on aip."plan_weather_connector_config" for each row execute function aip.touch_updated_at();

-- VE_Component_Master · Value Exposure · business key: Component_ID
create table if not exists aip."ve_component_master" (
  tenant_id uuid not null references aip.tenants(id) on delete cascade,
  row_key text not null,
  "component_id" varchar(200),
  "component_name" varchar(200),
  "domain_owner" varchar(200),
  "calculation_method" varchar(200),
  "definition" varchar(1000),
  "primary_source_module" varchar(200),
  "authority_type" varchar(200),
  "double_count_check" varchar(200),
  "time_rule_id" varchar(200),
  "governance_rule" varchar(200),
  created_at timestamptz not null default now(),
  updated_at timestamptz not null default now(),
  primary key (tenant_id, row_key)
);
comment on table aip."ve_component_master" is 'Workbook sheet "VE_Component_Master"';
alter table aip."ve_component_master" enable row level security;
drop policy if exists p_read on aip."ve_component_master";
create policy p_read on aip."ve_component_master" for select to authenticated using (aip.has_role(tenant_id));
drop policy if exists p_write on aip."ve_component_master";
create policy p_write on aip."ve_component_master" for all to authenticated
  using (aip.has_role(tenant_id, array['planner','admin'])) with check (aip.has_role(tenant_id, array['planner','admin']));
drop trigger if exists t_audit on aip."ve_component_master";
create trigger t_audit after insert or update or delete on aip."ve_component_master" for each row execute function aip.audit_row();
drop trigger if exists t_touch on aip."ve_component_master";
create trigger t_touch before update on aip."ve_component_master" for each row execute function aip.touch_updated_at();

-- VE_Rate_Config · Value Exposure · business key: Rate_ID
create table if not exists aip."ve_rate_config" (
  tenant_id uuid not null references aip.tenants(id) on delete cascade,
  row_key text not null,
  "rate_id" varchar(200),
  "rate_type" varchar(200),
  "applies_to" varchar(200),
  "rate_or_value" numeric(24,10),
  "unit" varchar(200),
  "source_system" varchar(200),
  "source_object" varchar(200),
  "authority_type" varchar(200),
  "effective_from" timestamp without time zone,
  "effective_to" timestamp without time zone,
  "last_refresh" timestamp without time zone,
  "freshness_status" varchar(200),
  "usage" varchar(200),
  created_at timestamptz not null default now(),
  updated_at timestamptz not null default now(),
  primary key (tenant_id, row_key)
);
comment on table aip."ve_rate_config" is 'Workbook sheet "VE_Rate_Config"';
alter table aip."ve_rate_config" enable row level security;
drop policy if exists p_read on aip."ve_rate_config";
create policy p_read on aip."ve_rate_config" for select to authenticated using (aip.has_role(tenant_id));
drop policy if exists p_write on aip."ve_rate_config";
create policy p_write on aip."ve_rate_config" for all to authenticated
  using (aip.has_role(tenant_id, array['planner','admin'])) with check (aip.has_role(tenant_id, array['planner','admin']));
drop trigger if exists t_audit on aip."ve_rate_config";
create trigger t_audit after insert or update or delete on aip."ve_rate_config" for each row execute function aip.audit_row();
drop trigger if exists t_touch on aip."ve_rate_config";
create trigger t_touch before update on aip."ve_rate_config" for each row execute function aip.touch_updated_at();

-- VE_Time_Rules · Value Exposure · business key: Time_Rule_ID
create table if not exists aip."ve_time_rules" (
  tenant_id uuid not null references aip.tenants(id) on delete cascade,
  row_key text not null,
  "time_rule_id" varchar(200),
  "rule_type" varchar(200),
  "rule_definition" varchar(200),
  "formula_logic" varchar(200),
  "behavior" varchar(200),
  "baseline_date" varchar(200),
  "target_date" varchar(200),
  "threshold_triggered" varchar(200),
  created_at timestamptz not null default now(),
  updated_at timestamptz not null default now(),
  primary key (tenant_id, row_key)
);
comment on table aip."ve_time_rules" is 'Workbook sheet "VE_Time_Rules"';
alter table aip."ve_time_rules" enable row level security;
drop policy if exists p_read on aip."ve_time_rules";
create policy p_read on aip."ve_time_rules" for select to authenticated using (aip.has_role(tenant_id));
drop policy if exists p_write on aip."ve_time_rules";
create policy p_write on aip."ve_time_rules" for all to authenticated
  using (aip.has_role(tenant_id, array['planner','admin'])) with check (aip.has_role(tenant_id, array['planner','admin']));
drop trigger if exists t_audit on aip."ve_time_rules";
create trigger t_audit after insert or update or delete on aip."ve_time_rules" for each row execute function aip.audit_row();
drop trigger if exists t_touch on aip."ve_time_rules";
create trigger t_touch before update on aip."ve_time_rules" for each row execute function aip.touch_updated_at();

-- SUS_Metric_Catalogue · Sustainability & Portfolio · business key: Metric_ID
create table if not exists aip."sus_metric_catalogue" (
  tenant_id uuid not null references aip.tenants(id) on delete cascade,
  row_key text not null,
  "metric_id" varchar(200),
  "metric_name" varchar(200),
  "definition" varchar(200),
  "formula" varchar(200),
  "unit" varchar(200),
  "asset_relevance" varchar(200),
  "applicable_asset_scope" varchar(200),
  "frequency" varchar(200),
  "aip_applicability" varchar(200),
  created_at timestamptz not null default now(),
  updated_at timestamptz not null default now(),
  primary key (tenant_id, row_key)
);
comment on table aip."sus_metric_catalogue" is 'Workbook sheet "SUS_Metric_Catalogue"';
alter table aip."sus_metric_catalogue" enable row level security;
drop policy if exists p_read on aip."sus_metric_catalogue";
create policy p_read on aip."sus_metric_catalogue" for select to authenticated using (aip.has_role(tenant_id));
drop policy if exists p_write on aip."sus_metric_catalogue";
create policy p_write on aip."sus_metric_catalogue" for all to authenticated
  using (aip.has_role(tenant_id, array['planner','admin'])) with check (aip.has_role(tenant_id, array['planner','admin']));
drop trigger if exists t_audit on aip."sus_metric_catalogue";
create trigger t_audit after insert or update or delete on aip."sus_metric_catalogue" for each row execute function aip.audit_row();
drop trigger if exists t_touch on aip."sus_metric_catalogue";
create trigger t_touch before update on aip."sus_metric_catalogue" for each row execute function aip.touch_updated_at();

-- SUS_Framework_Mapping · Sustainability & Portfolio · business key: Metric_ID + Framework
create table if not exists aip."sus_framework_mapping" (
  tenant_id uuid not null references aip.tenants(id) on delete cascade,
  row_key text not null,
  "metric_id" varchar(200),
  "framework" varchar(200),
  "disclosure_id" varchar(200),
  "applicable" varchar(200),
  "requirement_or_link" varchar(200),
  "version" varchar(200),
  "effective_from" date,
  "mapping_status" varchar(200),
  created_at timestamptz not null default now(),
  updated_at timestamptz not null default now(),
  primary key (tenant_id, row_key)
);
comment on table aip."sus_framework_mapping" is 'Workbook sheet "SUS_Framework_Mapping"';
alter table aip."sus_framework_mapping" enable row level security;
drop policy if exists p_read on aip."sus_framework_mapping";
create policy p_read on aip."sus_framework_mapping" for select to authenticated using (aip.has_role(tenant_id));
drop policy if exists p_write on aip."sus_framework_mapping";
create policy p_write on aip."sus_framework_mapping" for all to authenticated
  using (aip.has_role(tenant_id, array['planner','admin'])) with check (aip.has_role(tenant_id, array['planner','admin']));
drop trigger if exists t_audit on aip."sus_framework_mapping";
create trigger t_audit after insert or update or delete on aip."sus_framework_mapping" for each row execute function aip.audit_row();
drop trigger if exists t_touch on aip."sus_framework_mapping";
create trigger t_touch before update on aip."sus_framework_mapping" for each row execute function aip.touch_updated_at();

-- SUS_Source_Registry · Sustainability & Portfolio · business key: Source_ID
create table if not exists aip."sus_source_registry" (
  tenant_id uuid not null references aip.tenants(id) on delete cascade,
  row_key text not null,
  "source_id" varchar(200),
  "source_type" varchar(200),
  "authority" varchar(200),
  "country" varchar(200),
  "region" varchar(200),
  "document_title" varchar(200),
  "document_version" varchar(200),
  "publication_date" varchar(200),
  "effective_from" varchar(200),
  "effective_to" varchar(200),
  "source_url" text,
  "retrieved_date" varchar(200),
  "status" varchar(200),
  "evidence_notes" varchar(200),
  created_at timestamptz not null default now(),
  updated_at timestamptz not null default now(),
  primary key (tenant_id, row_key)
);
comment on table aip."sus_source_registry" is 'Workbook sheet "SUS_Source_Registry"';
alter table aip."sus_source_registry" enable row level security;
drop policy if exists p_read on aip."sus_source_registry";
create policy p_read on aip."sus_source_registry" for select to authenticated using (aip.has_role(tenant_id));
drop policy if exists p_write on aip."sus_source_registry";
create policy p_write on aip."sus_source_registry" for all to authenticated
  using (aip.has_role(tenant_id, array['planner','admin'])) with check (aip.has_role(tenant_id, array['planner','admin']));
drop trigger if exists t_audit on aip."sus_source_registry";
create trigger t_audit after insert or update or delete on aip."sus_source_registry" for each row execute function aip.audit_row();
drop trigger if exists t_touch on aip."sus_source_registry";
create trigger t_touch before update on aip."sus_source_registry" for each row execute function aip.touch_updated_at();

-- SUS_Generation · Sustainability & Portfolio · business key: Plant_ID
create table if not exists aip."sus_generation" (
  tenant_id uuid not null references aip.tenants(id) on delete cascade,
  row_key text not null,
  "period" varchar(200),
  "plant_id" varchar(200),
  "plant_name" varchar(200),
  "state" varchar(200),
  "mwac" integer,
  "expected_mwh" integer,
  "net_mwh" numeric(24,2),
  "lost_mwh" numeric(24,2),
  "recovered_mwh" numeric(24,2),
  "source_system" varchar(200),
  "data_status" varchar(200),
  created_at timestamptz not null default now(),
  updated_at timestamptz not null default now(),
  primary key (tenant_id, row_key)
);
comment on table aip."sus_generation" is 'Workbook sheet "SUS_Generation"';
alter table aip."sus_generation" enable row level security;
drop policy if exists p_read on aip."sus_generation";
create policy p_read on aip."sus_generation" for select to authenticated using (aip.has_role(tenant_id));
drop policy if exists p_write on aip."sus_generation";
create policy p_write on aip."sus_generation" for all to authenticated
  using (aip.has_role(tenant_id, array['planner','admin'])) with check (aip.has_role(tenant_id, array['planner','admin']));
drop trigger if exists t_audit on aip."sus_generation";
create trigger t_audit after insert or update or delete on aip."sus_generation" for each row execute function aip.audit_row();
drop trigger if exists t_touch on aip."sus_generation";
create trigger t_touch before update on aip."sus_generation" for each row execute function aip.touch_updated_at();

-- SUS_Water_Cleaning · Sustainability & Portfolio · business key: Cleaning_Event_ID
create table if not exists aip."sus_water_cleaning" (
  tenant_id uuid not null references aip.tenants(id) on delete cascade,
  row_key text not null,
  "cleaning_event_id" varchar(200),
  "date" date,
  "plant_id" varchar(200),
  "plant_name" varchar(200),
  "pv_array_id" varchar(200),
  "pv_array_name" varchar(200),
  "asset_id" varchar(200),
  "asset_class" varchar(200),
  "cleaning_type" varchar(200),
  "water_m3" numeric(24,2),
  "soiling_before_pct" numeric(24,2),
  "soiling_after_pct" numeric(24,2),
  "mwh_recovered" numeric(24,2),
  "water_per_recovered_mwh_l" numeric(24,2),
  "source_system" varchar(200),
  "data_status" varchar(200),
  created_at timestamptz not null default now(),
  updated_at timestamptz not null default now(),
  primary key (tenant_id, row_key)
);
comment on table aip."sus_water_cleaning" is 'Workbook sheet "SUS_Water_Cleaning"';
alter table aip."sus_water_cleaning" enable row level security;
drop policy if exists p_read on aip."sus_water_cleaning";
create policy p_read on aip."sus_water_cleaning" for select to authenticated using (aip.has_role(tenant_id));
drop policy if exists p_write on aip."sus_water_cleaning";
create policy p_write on aip."sus_water_cleaning" for all to authenticated
  using (aip.has_role(tenant_id, array['planner','admin'])) with check (aip.has_role(tenant_id, array['planner','admin']));
drop trigger if exists t_audit on aip."sus_water_cleaning";
create trigger t_audit after insert or update or delete on aip."sus_water_cleaning" for each row execute function aip.audit_row();
drop trigger if exists t_touch on aip."sus_water_cleaning";
create trigger t_touch before update on aip."sus_water_cleaning" for each row execute function aip.touch_updated_at();

-- SUS_GHG_Activity · Sustainability & Portfolio · business key: Activity_ID
create table if not exists aip."sus_ghg_activity" (
  tenant_id uuid not null references aip.tenants(id) on delete cascade,
  row_key text not null,
  "activity_id" varchar(200),
  "period" varchar(200),
  "plant_id" varchar(200),
  "asset_id" varchar(200),
  "asset_class" varchar(200),
  "ghg_scope" varchar(200),
  "emission_source" varchar(200),
  "activity_value" numeric(24,2),
  "activity_unit" varchar(200),
  "factor_id" varchar(200),
  "factor_value" varchar(200),
  "factor_unit" varchar(200),
  "calculated_t_co2e" varchar(200),
  "source_system" varchar(200),
  "data_status" varchar(200),
  created_at timestamptz not null default now(),
  updated_at timestamptz not null default now(),
  primary key (tenant_id, row_key)
);
comment on table aip."sus_ghg_activity" is 'Workbook sheet "SUS_GHG_Activity"';
alter table aip."sus_ghg_activity" enable row level security;
drop policy if exists p_read on aip."sus_ghg_activity";
create policy p_read on aip."sus_ghg_activity" for select to authenticated using (aip.has_role(tenant_id));
drop policy if exists p_write on aip."sus_ghg_activity";
create policy p_write on aip."sus_ghg_activity" for all to authenticated
  using (aip.has_role(tenant_id, array['planner','admin'])) with check (aip.has_role(tenant_id, array['planner','admin']));
drop trigger if exists t_audit on aip."sus_ghg_activity";
create trigger t_audit after insert or update or delete on aip."sus_ghg_activity" for each row execute function aip.audit_row();
drop trigger if exists t_touch on aip."sus_ghg_activity";
create trigger t_touch before update on aip."sus_ghg_activity" for each row execute function aip.touch_updated_at();

-- SUS_Climate_Asset_Risk · Sustainability & Portfolio · business key: Risk_ID
create table if not exists aip."sus_climate_asset_risk" (
  tenant_id uuid not null references aip.tenants(id) on delete cascade,
  row_key text not null,
  "risk_id" varchar(200),
  "plant_id" varchar(200),
  "asset_id" varchar(200),
  "asset_class" varchar(200),
  "hazard" varchar(200),
  "exposure" varchar(200),
  "vulnerability_basis" varchar(200),
  "operational_impact" varchar(200),
  "maintenance_response" varchar(200),
  "source_id" varchar(200),
  "data_status" varchar(200),
  "exposure_score" integer,
  "vulnerability_score" integer,
  "operational_consequence_score" integer,
  "response_readiness_status" varchar(200),
  "response_readiness_gap_score" integer,
  "exposure_weighted_contribution" numeric(24,2),
  "vulnerability_weighted_contribution" integer,
  "operational_consequence_weighted_co_a2e2" integer,
  "response_readiness_weighted_contribution" integer,
  "climate_priority_score" numeric(24,2),
  "climate_priority_band" varchar(200),
  "climate_priority_formula_trace" varchar(200),
  created_at timestamptz not null default now(),
  updated_at timestamptz not null default now(),
  primary key (tenant_id, row_key)
);
comment on table aip."sus_climate_asset_risk" is 'Workbook sheet "SUS_Climate_Asset_Risk"';
alter table aip."sus_climate_asset_risk" enable row level security;
drop policy if exists p_read on aip."sus_climate_asset_risk";
create policy p_read on aip."sus_climate_asset_risk" for select to authenticated using (aip.has_role(tenant_id));
drop policy if exists p_write on aip."sus_climate_asset_risk";
create policy p_write on aip."sus_climate_asset_risk" for all to authenticated
  using (aip.has_role(tenant_id, array['planner','admin'])) with check (aip.has_role(tenant_id, array['planner','admin']));
drop trigger if exists t_audit on aip."sus_climate_asset_risk";
create trigger t_audit after insert or update or delete on aip."sus_climate_asset_risk" for each row execute function aip.audit_row();
drop trigger if exists t_touch on aip."sus_climate_asset_risk";
create trigger t_touch before update on aip."sus_climate_asset_risk" for each row execute function aip.touch_updated_at();

-- SUS_HSE_Events · Sustainability & Portfolio · business key: Event_ID
create table if not exists aip."sus_hse_events" (
  tenant_id uuid not null references aip.tenants(id) on delete cascade,
  row_key text not null,
  "event_id" varchar(200),
  "date" date,
  "plant_id" varchar(200),
  "asset_id" varchar(200),
  "work_order_id" varchar(200),
  "asset_class" varchar(200),
  "maintenance_activity" varchar(200),
  "worker_type" varchar(200),
  "event_type" varchar(200),
  "severity" varchar(200),
  "status" varchar(200),
  "source_system" varchar(200),
  "data_status" varchar(200),
  created_at timestamptz not null default now(),
  updated_at timestamptz not null default now(),
  primary key (tenant_id, row_key)
);
comment on table aip."sus_hse_events" is 'Workbook sheet "SUS_HSE_Events"';
alter table aip."sus_hse_events" enable row level security;
drop policy if exists p_read on aip."sus_hse_events";
create policy p_read on aip."sus_hse_events" for select to authenticated using (aip.has_role(tenant_id));
drop policy if exists p_write on aip."sus_hse_events";
create policy p_write on aip."sus_hse_events" for all to authenticated
  using (aip.has_role(tenant_id, array['planner','admin'])) with check (aip.has_role(tenant_id, array['planner','admin']));
drop trigger if exists t_audit on aip."sus_hse_events";
create trigger t_audit after insert or update or delete on aip."sus_hse_events" for each row execute function aip.audit_row();
drop trigger if exists t_touch on aip."sus_hse_events";
create trigger t_touch before update on aip."sus_hse_events" for each row execute function aip.touch_updated_at();

-- SUS_HSE_Hours · Sustainability & Portfolio · business key: Period + Plant_ID + Worker_Type
create table if not exists aip."sus_hse_hours" (
  tenant_id uuid not null references aip.tenants(id) on delete cascade,
  row_key text not null,
  "period" varchar(200),
  "plant_id" varchar(200),
  "worker_type" varchar(200),
  "hours_worked" integer,
  "source_system" varchar(200),
  "data_status" varchar(200),
  created_at timestamptz not null default now(),
  updated_at timestamptz not null default now(),
  primary key (tenant_id, row_key)
);
comment on table aip."sus_hse_hours" is 'Workbook sheet "SUS_HSE_Hours"';
alter table aip."sus_hse_hours" enable row level security;
drop policy if exists p_read on aip."sus_hse_hours";
create policy p_read on aip."sus_hse_hours" for select to authenticated using (aip.has_role(tenant_id));
drop policy if exists p_write on aip."sus_hse_hours";
create policy p_write on aip."sus_hse_hours" for all to authenticated
  using (aip.has_role(tenant_id, array['planner','admin'])) with check (aip.has_role(tenant_id, array['planner','admin']));
drop trigger if exists t_audit on aip."sus_hse_hours";
create trigger t_audit after insert or update or delete on aip."sus_hse_hours" for each row execute function aip.audit_row();
drop trigger if exists t_touch on aip."sus_hse_hours";
create trigger t_touch before update on aip."sus_hse_hours" for each row execute function aip.touch_updated_at();

-- SUS_Circularity_EOL · Sustainability & Portfolio · business key: EOL_Record_ID
create table if not exists aip."sus_circularity_eol" (
  tenant_id uuid not null references aip.tenants(id) on delete cascade,
  row_key text not null,
  "eol_record_id" varchar(200),
  "plant_id" varchar(200),
  "asset_id" varchar(200),
  "asset_class" varchar(200),
  "commission_year" integer,
  "age_years" integer,
  "condition" varchar(200),
  "rul_years" integer,
  "eol_flag" varchar(200),
  "replacement_driver" varchar(200),
  "quantity" integer,
  "unit" varchar(200),
  "recovery_path" varchar(200),
  "regulatory_status" varchar(200),
  "source_system" varchar(200),
  "data_status" varchar(200),
  "record_grain" varchar(200),
  "eol_horizon_years" integer,
  "pathway_status" varchar(200),
  "unit_mass_kg" integer,
  "total_mass_kg" integer,
  "recovery_rate_pct" integer,
  "recoverable_mass_kg" integer,
  "replacement_unit_cost" numeric(24,2),
  "replacement_exposure" numeric(24,2),
  "recovery_process_cost_per_kg" numeric(24,2),
  "gross_recovered_value_per_kg" numeric(24,2),
  "net_recovery_value" numeric(24,2),
  "assumption_profile_id" varchar(200),
  "data_basis" varchar(200),
  "external_source_id" varchar(200),
  "data_enhancement_score_pct" integer,
  "kpi_eligible" varchar(200),
  "eol_priority_score" numeric(24,2),
  "eol_priority_band" varchar(200),
  "formula_trace" varchar(1000),
  currency char(3) not null default 'INR' check (currency ~ '^[A-Z]{3}$'),
  created_at timestamptz not null default now(),
  updated_at timestamptz not null default now(),
  primary key (tenant_id, row_key)
);
comment on table aip."sus_circularity_eol" is 'Workbook sheet "SUS_Circularity_EOL"';
alter table aip."sus_circularity_eol" enable row level security;
drop policy if exists p_read on aip."sus_circularity_eol";
create policy p_read on aip."sus_circularity_eol" for select to authenticated using (aip.has_role(tenant_id));
drop policy if exists p_write on aip."sus_circularity_eol";
create policy p_write on aip."sus_circularity_eol" for all to authenticated
  using (aip.has_role(tenant_id, array['planner','admin'])) with check (aip.has_role(tenant_id, array['planner','admin']));
drop trigger if exists t_audit on aip."sus_circularity_eol";
create trigger t_audit after insert or update or delete on aip."sus_circularity_eol" for each row execute function aip.audit_row();
drop trigger if exists t_touch on aip."sus_circularity_eol";
create trigger t_touch before update on aip."sus_circularity_eol" for each row execute function aip.touch_updated_at();

-- SUS_Benchmarks · Sustainability & Portfolio · business key: Benchmark_ID
create table if not exists aip."sus_benchmarks" (
  tenant_id uuid not null references aip.tenants(id) on delete cascade,
  row_key text not null,
  "benchmark_id" varchar(200),
  "metric_id" varchar(200),
  "benchmark_type" varchar(200),
  "geography_technology" varchar(200),
  "value" varchar(200),
  "unit" varchar(200),
  "range_low" varchar(200),
  "range_high" varchar(200),
  "source_id" varchar(200),
  "effective_period" varchar(200),
  "calculation_basis" varchar(200),
  "notes" varchar(200),
  created_at timestamptz not null default now(),
  updated_at timestamptz not null default now(),
  primary key (tenant_id, row_key)
);
comment on table aip."sus_benchmarks" is 'Workbook sheet "SUS_Benchmarks"';
alter table aip."sus_benchmarks" enable row level security;
drop policy if exists p_read on aip."sus_benchmarks";
create policy p_read on aip."sus_benchmarks" for select to authenticated using (aip.has_role(tenant_id));
drop policy if exists p_write on aip."sus_benchmarks";
create policy p_write on aip."sus_benchmarks" for all to authenticated
  using (aip.has_role(tenant_id, array['planner','admin'])) with check (aip.has_role(tenant_id, array['planner','admin']));
drop trigger if exists t_audit on aip."sus_benchmarks";
create trigger t_audit after insert or update or delete on aip."sus_benchmarks" for each row execute function aip.audit_row();
drop trigger if exists t_touch on aip."sus_benchmarks";
create trigger t_touch before update on aip."sus_benchmarks" for each row execute function aip.touch_updated_at();

-- SUS_Connector_Registry · Sustainability & Portfolio · business key: Connector_ID
create table if not exists aip."sus_connector_registry" (
  tenant_id uuid not null references aip.tenants(id) on delete cascade,
  row_key text not null,
  "connector_id" varchar(200),
  "data_domain" varchar(200),
  "source" varchar(200),
  "connector_type" varchar(200),
  "status" varchar(200),
  "refresh_frequency" varchar(200),
  "data_content" varchar(200),
  created_at timestamptz not null default now(),
  updated_at timestamptz not null default now(),
  primary key (tenant_id, row_key)
);
comment on table aip."sus_connector_registry" is 'Workbook sheet "SUS_Connector_Registry"';
alter table aip."sus_connector_registry" enable row level security;
drop policy if exists p_read on aip."sus_connector_registry";
create policy p_read on aip."sus_connector_registry" for select to authenticated using (aip.has_role(tenant_id));
drop policy if exists p_write on aip."sus_connector_registry";
create policy p_write on aip."sus_connector_registry" for all to authenticated
  using (aip.has_role(tenant_id, array['planner','admin'])) with check (aip.has_role(tenant_id, array['planner','admin']));
drop trigger if exists t_audit on aip."sus_connector_registry";
create trigger t_audit after insert or update or delete on aip."sus_connector_registry" for each row execute function aip.audit_row();
drop trigger if exists t_touch on aip."sus_connector_registry";
create trigger t_touch before update on aip."sus_connector_registry" for each row execute function aip.touch_updated_at();

-- SUS_Model_Governance · Sustainability & Portfolio · business key: Assumption_Profile_ID + Parameter
create table if not exists aip."sus_model_governance" (
  tenant_id uuid not null references aip.tenants(id) on delete cascade,
  row_key text not null,
  "assumption_profile_id" varchar(200),
  "asset_class" varchar(200),
  "parameter" varchar(200),
  "value" varchar(200),
  "unit" varchar(200),
  "basis_type" varchar(200),
  "source_or_governance_basis" varchar(200),
  "source_url" text,
  "confidence_pct" integer,
  "governance_note" varchar(200),
  created_at timestamptz not null default now(),
  updated_at timestamptz not null default now(),
  primary key (tenant_id, row_key)
);
comment on table aip."sus_model_governance" is 'Workbook sheet "SUS_Model_Governance"';
alter table aip."sus_model_governance" enable row level security;
drop policy if exists p_read on aip."sus_model_governance";
create policy p_read on aip."sus_model_governance" for select to authenticated using (aip.has_role(tenant_id));
drop policy if exists p_write on aip."sus_model_governance";
create policy p_write on aip."sus_model_governance" for all to authenticated
  using (aip.has_role(tenant_id, array['planner','admin'])) with check (aip.has_role(tenant_id, array['planner','admin']));
drop trigger if exists t_audit on aip."sus_model_governance";
create trigger t_audit after insert or update or delete on aip."sus_model_governance" for each row execute function aip.audit_row();
drop trigger if exists t_touch on aip."sus_model_governance";
create trigger t_touch before update on aip."sus_model_governance" for each row execute function aip.touch_updated_at();

-- SUS_PV_Array_Master · Sustainability & Portfolio · business key: KPI_ID
create table if not exists aip."sus_pv_array_master" (
  tenant_id uuid not null references aip.tenants(id) on delete cascade,
  row_key text not null,
  "kpi_id" varchar(200),
  "kpi_name" varchar(200),
  "excel_value" numeric(24,2),
  "unit" varchar(200),
  "numerator_or_value_basis" varchar(200),
  "denominator" varchar(200),
  "formula" varchar(200),
  "source_fields" varchar(200),
  "drilldown_filter" varchar(200),
  "assumption_disclosure" varchar(1000),
  created_at timestamptz not null default now(),
  updated_at timestamptz not null default now(),
  primary key (tenant_id, row_key)
);
comment on table aip."sus_pv_array_master" is 'Workbook sheet "SUS_PV_Array_Master"';
alter table aip."sus_pv_array_master" enable row level security;
drop policy if exists p_read on aip."sus_pv_array_master";
create policy p_read on aip."sus_pv_array_master" for select to authenticated using (aip.has_role(tenant_id));
drop policy if exists p_write on aip."sus_pv_array_master";
create policy p_write on aip."sus_pv_array_master" for all to authenticated
  using (aip.has_role(tenant_id, array['planner','admin'])) with check (aip.has_role(tenant_id, array['planner','admin']));
drop trigger if exists t_audit on aip."sus_pv_array_master";
create trigger t_audit after insert or update or delete on aip."sus_pv_array_master" for each row execute function aip.audit_row();
drop trigger if exists t_touch on aip."sus_pv_array_master";
create trigger t_touch before update on aip."sus_pv_array_master" for each row execute function aip.touch_updated_at();

-- SUS_EOL_Assumptions · Sustainability & Portfolio · business key: Assumption_Profile_ID + Parameter
create table if not exists aip."sus_eol_assumptions" (
  tenant_id uuid not null references aip.tenants(id) on delete cascade,
  row_key text not null,
  "assumption_profile_id" varchar(200),
  "asset_class" varchar(200),
  "parameter" varchar(200),
  "value" varchar(200),
  "unit" varchar(200),
  "basis_type" varchar(200),
  "source_or_governance_basis" varchar(200),
  "source_url" text,
  "confidence_pct" integer,
  "governance_note" varchar(200),
  created_at timestamptz not null default now(),
  updated_at timestamptz not null default now(),
  primary key (tenant_id, row_key)
);
comment on table aip."sus_eol_assumptions" is 'Workbook sheet "SUS_EOL_Assumptions"';
alter table aip."sus_eol_assumptions" enable row level security;
drop policy if exists p_read on aip."sus_eol_assumptions";
create policy p_read on aip."sus_eol_assumptions" for select to authenticated using (aip.has_role(tenant_id));
drop policy if exists p_write on aip."sus_eol_assumptions";
create policy p_write on aip."sus_eol_assumptions" for all to authenticated
  using (aip.has_role(tenant_id, array['planner','admin'])) with check (aip.has_role(tenant_id, array['planner','admin']));
drop trigger if exists t_audit on aip."sus_eol_assumptions";
create trigger t_audit after insert or update or delete on aip."sus_eol_assumptions" for each row execute function aip.audit_row();
drop trigger if exists t_touch on aip."sus_eol_assumptions";
create trigger t_touch before update on aip."sus_eol_assumptions" for each row execute function aip.touch_updated_at();

-- SUS_EOL_KPI_Basis · Sustainability & Portfolio · business key: KPI_ID
create table if not exists aip."sus_eol_kpi_basis" (
  tenant_id uuid not null references aip.tenants(id) on delete cascade,
  row_key text not null,
  "kpi_id" varchar(200),
  "kpi_name" varchar(200),
  "excel_value" numeric(24,2),
  "unit" varchar(200),
  "numerator_or_value_basis" varchar(200),
  "denominator" varchar(200),
  "formula" varchar(200),
  "source_fields" varchar(200),
  "drilldown_filter" varchar(200),
  "assumption_disclosure" varchar(1000),
  created_at timestamptz not null default now(),
  updated_at timestamptz not null default now(),
  primary key (tenant_id, row_key)
);
comment on table aip."sus_eol_kpi_basis" is 'Workbook sheet "SUS_EOL_KPI_Basis"';
alter table aip."sus_eol_kpi_basis" enable row level security;
drop policy if exists p_read on aip."sus_eol_kpi_basis";
create policy p_read on aip."sus_eol_kpi_basis" for select to authenticated using (aip.has_role(tenant_id));
drop policy if exists p_write on aip."sus_eol_kpi_basis";
create policy p_write on aip."sus_eol_kpi_basis" for all to authenticated
  using (aip.has_role(tenant_id, array['planner','admin'])) with check (aip.has_role(tenant_id, array['planner','admin']));
drop trigger if exists t_audit on aip."sus_eol_kpi_basis";
create trigger t_audit after insert or update or delete on aip."sus_eol_kpi_basis" for each row execute function aip.audit_row();
drop trigger if exists t_touch on aip."sus_eol_kpi_basis";
create trigger t_touch before update on aip."sus_eol_kpi_basis" for each row execute function aip.touch_updated_at();

-- Operational Reliability · Maintenance & Reliability · business key: Reliability_ID
create table if not exists aip."operational_reliability" (
  tenant_id uuid not null references aip.tenants(id) on delete cascade,
  row_key text not null,
  "reliability_id" varchar(200),
  "record_type" varchar(200),
  "asset_class" varchar(200),
  "period" varchar(200),
  "mtbf_hours" numeric(24,2),
  "mttr_hours" numeric(24,2),
  "reliability_pct" numeric(24,2),
  "technical_availability_pct" numeric(24,2),
  "criticality" varchar(200),
  "trend_status" varchar(200),
  "failure_mode" varchar(200),
  "failure_share_pct" numeric(24,2),
  "reliability_index_points" numeric(24,2),
  "incident_id" varchar(200),
  "plant_id" varchar(200),
  "plant_name" varchar(200),
  "asset_id" varchar(200),
  "asset_tag" varchar(200),
  "incident_type" varchar(200),
  "severity" varchar(200),
  "priority_score" integer,
  "evidence_confidence_pct" integer,
  "value_at_stake" numeric(24,2),
  "energy_at_risk_mwh" numeric(24,2),
  "source_sheet" varchar(200),
  "source_record_id" varchar(200),
  "calculation_basis" varchar(200),
  currency char(3) not null default 'INR' check (currency ~ '^[A-Z]{3}$'),
  created_at timestamptz not null default now(),
  updated_at timestamptz not null default now(),
  primary key (tenant_id, row_key)
);
comment on table aip."operational_reliability" is 'Workbook sheet "Operational Reliability"';
alter table aip."operational_reliability" enable row level security;
drop policy if exists p_read on aip."operational_reliability";
create policy p_read on aip."operational_reliability" for select to authenticated using (aip.has_role(tenant_id));
drop policy if exists p_write on aip."operational_reliability";
create policy p_write on aip."operational_reliability" for all to authenticated
  using (aip.has_role(tenant_id, array['planner','admin'])) with check (aip.has_role(tenant_id, array['planner','admin']));
drop trigger if exists t_audit on aip."operational_reliability";
create trigger t_audit after insert or update or delete on aip."operational_reliability" for each row execute function aip.audit_row();
drop trigger if exists t_touch on aip."operational_reliability";
create trigger t_touch before update on aip."operational_reliability" for each row execute function aip.touch_updated_at();

-- RCM Strategy · Maintenance & Reliability · business key: RCM_ID
create table if not exists aip."rcm_strategy" (
  tenant_id uuid not null references aip.tenants(id) on delete cascade,
  row_key text not null,
  "rcm_id" varchar(200),
  "asset_class" varchar(200),
  "failure_mode" varchar(200),
  "detection_method" varchar(200),
  "consequence" varchar(200),
  "maintenance_strategy" varchar(200),
  "rationale" varchar(1000),
  created_at timestamptz not null default now(),
  updated_at timestamptz not null default now(),
  primary key (tenant_id, row_key)
);
comment on table aip."rcm_strategy" is 'Workbook sheet "RCM Strategy"';
alter table aip."rcm_strategy" enable row level security;
drop policy if exists p_read on aip."rcm_strategy";
create policy p_read on aip."rcm_strategy" for select to authenticated using (aip.has_role(tenant_id));
drop policy if exists p_write on aip."rcm_strategy";
create policy p_write on aip."rcm_strategy" for all to authenticated
  using (aip.has_role(tenant_id, array['planner','admin'])) with check (aip.has_role(tenant_id, array['planner','admin']));
drop trigger if exists t_audit on aip."rcm_strategy";
create trigger t_audit after insert or update or delete on aip."rcm_strategy" for each row execute function aip.audit_row();
drop trigger if exists t_touch on aip."rcm_strategy";
create trigger t_touch before update on aip."rcm_strategy" for each row execute function aip.touch_updated_at();

-- RCM Policy Governance · Maintenance & Reliability · business key: RCM_ID
create table if not exists aip."rcm_policy_governance" (
  tenant_id uuid not null references aip.tenants(id) on delete cascade,
  row_key text not null,
  "rcm_id" varchar(200),
  "asset_class" varchar(200),
  "failure_mode" varchar(200),
  "policy_owner" varchar(200),
  "review_cycle" varchar(200),
  "policy_status" varchar(200),
  "outcome_validation" varchar(200),
  "change_trigger" varchar(1000),
  "standard_url" text,
  "pv_oand_m_url" text,
  "outcome_evidence_state" varchar(200),
  "matched_learning_reference" varchar(200),
  "matched_outcome_records" integer,
  "policy_review_state" varchar(200),
  "review_action" varchar(200),
  created_at timestamptz not null default now(),
  updated_at timestamptz not null default now(),
  primary key (tenant_id, row_key)
);
comment on table aip."rcm_policy_governance" is 'Workbook sheet "RCM Policy Governance"';
alter table aip."rcm_policy_governance" enable row level security;
drop policy if exists p_read on aip."rcm_policy_governance";
create policy p_read on aip."rcm_policy_governance" for select to authenticated using (aip.has_role(tenant_id));
drop policy if exists p_write on aip."rcm_policy_governance";
create policy p_write on aip."rcm_policy_governance" for all to authenticated
  using (aip.has_role(tenant_id, array['planner','admin'])) with check (aip.has_role(tenant_id, array['planner','admin']));
drop trigger if exists t_audit on aip."rcm_policy_governance";
create trigger t_audit after insert or update or delete on aip."rcm_policy_governance" for each row execute function aip.audit_row();
drop trigger if exists t_touch on aip."rcm_policy_governance";
create trigger t_touch before update on aip."rcm_policy_governance" for each row execute function aip.touch_updated_at();

-- CBM Assessments · Maintenance & Reliability · business key: CBM_Assessment_ID
create table if not exists aip."cbm_assessments" (
  tenant_id uuid not null references aip.tenants(id) on delete cascade,
  row_key text not null,
  "cbm_assessment_id" varchar(200),
  "plant_id" varchar(200),
  "plant_name" varchar(200),
  "asset_id" varchar(200),
  "asset_tag" varchar(200),
  "asset_class" varchar(200),
  "failure_mode" varchar(200),
  "primary_condition" varchar(200),
  "source_mode" varchar(200),
  "latest_evidence_time" timestamp without time zone,
  "freshness_class" varchar(200),
  "max_deviation_pct" numeric(24,2),
  "evidence_count" integer,
  "corroboration_count" integer,
  "condition_severity" varchar(200),
  "risk_context" varchar(200),
  "evidence_sufficiency_pct" numeric(24,2),
  "engineering_physics_fit_pct" numeric(24,2),
  "confidence_pct" numeric(24,2),
  "priority" varchar(200),
  "condition_state" varchar(200),
  "recommended_action" varchar(200),
  "governance_class" varchar(200),
  "approval_role" varchar(200),
  "source_module" varchar(200),
  "source_record_id" varchar(200),
  "target_view" varchar(200),
  "work_order_id" varchar(200),
  "reference_id" varchar(200),
  "verification_method" varchar(1000),
  "decision_basis" varchar(1000),
  created_at timestamptz not null default now(),
  updated_at timestamptz not null default now(),
  primary key (tenant_id, row_key)
);
comment on table aip."cbm_assessments" is 'Workbook sheet "CBM Assessments"';
alter table aip."cbm_assessments" enable row level security;
drop policy if exists p_read on aip."cbm_assessments";
create policy p_read on aip."cbm_assessments" for select to authenticated using (aip.has_role(tenant_id));
drop policy if exists p_write on aip."cbm_assessments";
create policy p_write on aip."cbm_assessments" for all to authenticated
  using (aip.has_role(tenant_id, array['planner','admin'])) with check (aip.has_role(tenant_id, array['planner','admin']));
drop trigger if exists t_audit on aip."cbm_assessments";
create trigger t_audit after insert or update or delete on aip."cbm_assessments" for each row execute function aip.audit_row();
drop trigger if exists t_touch on aip."cbm_assessments";
create trigger t_touch before update on aip."cbm_assessments" for each row execute function aip.touch_updated_at();

-- CBM Evidence · Maintenance & Reliability · business key: CBM_Evidence_ID
create table if not exists aip."cbm_evidence" (
  tenant_id uuid not null references aip.tenants(id) on delete cascade,
  row_key text not null,
  "cbm_evidence_id" varchar(200),
  "cbm_assessment_id" varchar(200),
  "plant_id" varchar(200),
  "asset_id" varchar(200),
  "evidence_source" varchar(200),
  "arrival_mode" varchar(200),
  "source_module" varchar(200),
  "target_view" varchar(200),
  "source_record_id" varchar(200),
  "observed_time" timestamp without time zone,
  "signal_name" varchar(200),
  "observed_value" numeric(24,2),
  "baseline_or_peer" numeric(24,2),
  "unit" varchar(200),
  "deviation_pct" numeric(24,2),
  "evidence_role" varchar(200),
  "quality_pct" numeric(24,2),
  "engineering_interpretation" varchar(1000),
  "freshness_label" varchar(200),
  created_at timestamptz not null default now(),
  updated_at timestamptz not null default now(),
  primary key (tenant_id, row_key)
);
comment on table aip."cbm_evidence" is 'Workbook sheet "CBM Evidence"';
alter table aip."cbm_evidence" enable row level security;
drop policy if exists p_read on aip."cbm_evidence";
create policy p_read on aip."cbm_evidence" for select to authenticated using (aip.has_role(tenant_id));
drop policy if exists p_write on aip."cbm_evidence";
create policy p_write on aip."cbm_evidence" for all to authenticated
  using (aip.has_role(tenant_id, array['planner','admin'])) with check (aip.has_role(tenant_id, array['planner','admin']));
drop trigger if exists t_audit on aip."cbm_evidence";
create trigger t_audit after insert or update or delete on aip."cbm_evidence" for each row execute function aip.audit_row();
drop trigger if exists t_touch on aip."cbm_evidence";
create trigger t_touch before update on aip."cbm_evidence" for each row execute function aip.touch_updated_at();

-- CBM Governance · Maintenance & Reliability · business key: Governance_ID
create table if not exists aip."cbm_governance" (
  tenant_id uuid not null references aip.tenants(id) on delete cascade,
  row_key text not null,
  "governance_id" varchar(200),
  "action_class" varchar(200),
  "governance_class" varchar(200),
  "automation_boundary" varchar(200),
  "required_approval" varchar(200),
  "erp_eam_action" varchar(200),
  "control" varchar(200),
  created_at timestamptz not null default now(),
  updated_at timestamptz not null default now(),
  primary key (tenant_id, row_key)
);
comment on table aip."cbm_governance" is 'Workbook sheet "CBM Governance"';
alter table aip."cbm_governance" enable row level security;
drop policy if exists p_read on aip."cbm_governance";
create policy p_read on aip."cbm_governance" for select to authenticated using (aip.has_role(tenant_id));
drop policy if exists p_write on aip."cbm_governance";
create policy p_write on aip."cbm_governance" for all to authenticated
  using (aip.has_role(tenant_id, array['planner','admin'])) with check (aip.has_role(tenant_id, array['planner','admin']));
drop trigger if exists t_audit on aip."cbm_governance";
create trigger t_audit after insert or update or delete on aip."cbm_governance" for each row execute function aip.audit_row();
drop trigger if exists t_touch on aip."cbm_governance";
create trigger t_touch before update on aip."cbm_governance" for each row execute function aip.touch_updated_at();

-- Risk Queue · Maintenance & Reliability · business key: Alert_ID
create table if not exists aip."risk_queue" (
  tenant_id uuid not null references aip.tenants(id) on delete cascade,
  row_key text not null,
  "queue_rank" integer,
  "alert_id" varchar(200),
  "asset_id" varchar(200),
  "plant_id" varchar(200),
  "asset_tag" varchar(200),
  "rated_capacity_mw" numeric(24,2),
  "exposure_hours" integer,
  "expected_derate_pct" numeric(24,2),
  "energy_impact_mwh" numeric(24,2),
  "safety_likelihood_1_5" integer,
  "safety_severity_1_5" integer,
  "safety_risk_rating_1_25" integer,
  "accessibility_1_10" numeric(24,2),
  "energy_impact_1_10" numeric(24,2),
  "safety_risk_1_10" numeric(24,2),
  "composite_risk" numeric(24,4),
  "priority_tier" varchar(200),
  created_at timestamptz not null default now(),
  updated_at timestamptz not null default now(),
  primary key (tenant_id, row_key)
);
comment on table aip."risk_queue" is 'Workbook sheet "Risk Queue"';
alter table aip."risk_queue" enable row level security;
drop policy if exists p_read on aip."risk_queue";
create policy p_read on aip."risk_queue" for select to authenticated using (aip.has_role(tenant_id));
drop policy if exists p_write on aip."risk_queue";
create policy p_write on aip."risk_queue" for all to authenticated
  using (aip.has_role(tenant_id, array['planner','admin'])) with check (aip.has_role(tenant_id, array['planner','admin']));
drop trigger if exists t_audit on aip."risk_queue";
create trigger t_audit after insert or update or delete on aip."risk_queue" for each row execute function aip.audit_row();
drop trigger if exists t_touch on aip."risk_queue";
create trigger t_touch before update on aip."risk_queue" for each row execute function aip.touch_updated_at();

-- Technology_Integration_Registry · Governance & Technology · business key: Connector_ID
create table if not exists aip."technology_integration_registry" (
  tenant_id uuid not null references aip.tenants(id) on delete cascade,
  row_key text not null,
  "connector_id" varchar(200),
  "domain" varchar(200),
  "source_system" varchar(200),
  "source_type" varchar(200),
  "objects_or_content" varchar(200),
  "aip_target" varchar(200),
  "integration_pattern" varchar(200),
  "status" varchar(200),
  "freshness_or_cadence" varchar(200),
  "source_authority" varchar(200),
  "credential_or_control" varchar(200),
  "lineage_key" varchar(200),
  "demo_note" varchar(200),
  created_at timestamptz not null default now(),
  updated_at timestamptz not null default now(),
  primary key (tenant_id, row_key)
);
comment on table aip."technology_integration_registry" is 'Workbook sheet "Technology_Integration_Registry"';
alter table aip."technology_integration_registry" enable row level security;
drop policy if exists p_read on aip."technology_integration_registry";
create policy p_read on aip."technology_integration_registry" for select to authenticated using (aip.has_role(tenant_id));
drop policy if exists p_write on aip."technology_integration_registry";
create policy p_write on aip."technology_integration_registry" for all to authenticated
  using (aip.has_role(tenant_id, array['planner','admin'])) with check (aip.has_role(tenant_id, array['planner','admin']));
drop trigger if exists t_audit on aip."technology_integration_registry";
create trigger t_audit after insert or update or delete on aip."technology_integration_registry" for each row execute function aip.audit_row();
drop trigger if exists t_touch on aip."technology_integration_registry";
create trigger t_touch before update on aip."technology_integration_registry" for each row execute function aip.touch_updated_at();

-- Twin Engineering Parameters · Forecast & Twin · business key: Plant_ID
create table if not exists aip."twin_engineering_parameters" (
  tenant_id uuid not null references aip.tenants(id) on delete cascade,
  row_key text not null,
  "plant_id" varchar(200),
  "plant_name" varchar(200),
  "state" varchar(200),
  "ac_capacity_mw" integer,
  "dc_ac_ratio" numeric(24,2),
  "module_temp_coeff_pct_per_c" numeric(24,2),
  "noct_c" integer,
  "base_dc_loss_pct" numeric(24,2),
  "base_ac_loss_pct" numeric(24,2),
  "transformer_efficiency_pct" numeric(24,2),
  "inverter_efficiency_pct" numeric(24,2),
  "annual_degradation_pct" numeric(24,2),
  "soiling_base_pct" numeric(24,2),
  "poa_reference_wm2" integer,
  "model_version" varchar(200),
  "model_status" varchar(200),
  "latitude_deg" numeric(24,2),
  "longitude_deg" numeric(24,2),
  "array_tilt_deg" integer,
  "array_azimuth_deg" integer,
  "solar_window_start_local" varchar(200),
  "solar_window_end_local" varchar(200),
  "solar_window_source" varchar(200),
  "solar_window_method" varchar(200),
  created_at timestamptz not null default now(),
  updated_at timestamptz not null default now(),
  primary key (tenant_id, row_key)
);
comment on table aip."twin_engineering_parameters" is 'Workbook sheet "Twin Engineering Parameters"';
alter table aip."twin_engineering_parameters" enable row level security;
drop policy if exists p_read on aip."twin_engineering_parameters";
create policy p_read on aip."twin_engineering_parameters" for select to authenticated using (aip.has_role(tenant_id));
drop policy if exists p_write on aip."twin_engineering_parameters";
create policy p_write on aip."twin_engineering_parameters" for all to authenticated
  using (aip.has_role(tenant_id, array['planner','admin'])) with check (aip.has_role(tenant_id, array['planner','admin']));
drop trigger if exists t_audit on aip."twin_engineering_parameters";
create trigger t_audit after insert or update or delete on aip."twin_engineering_parameters" for each row execute function aip.audit_row();
drop trigger if exists t_touch on aip."twin_engineering_parameters";
create trigger t_touch before update on aip."twin_engineering_parameters" for each row execute function aip.touch_updated_at();

-- Twin Forecast Governance · Forecast & Twin · business key: Plant_ID + Horizon_Hours
create table if not exists aip."twin_forecast_governance" (
  tenant_id uuid not null references aip.tenants(id) on delete cascade,
  row_key text not null,
  "plant_id" varchar(200),
  "horizon_hours" integer,
  "forecast_start" timestamp without time zone,
  "forecast_end" timestamp without time zone,
  "forecast_delivered_mwh" numeric(24,2),
  "weather_adjusted_baseline_mwh" numeric(24,2),
  "clear_sky_reference_mwh" numeric(24,2),
  "operational_energy_at_risk_mwh" numeric(24,2),
  "average_forecast_cloud_pct_daylight" numeric(24,2),
  "peak_poa_wm2" numeric(24,2),
  "average_ambient_temp_c_daylight" numeric(24,2),
  "average_wind_speed_ms_daylight" numeric(24,2),
  "average_inverter_efficiency_pct_daylight" numeric(24,2),
  "average_availability_pct_daylight" numeric(24,2),
  "average_soiling_pct_daylight" numeric(24,2),
  "average_curtailment_pct_daylight" numeric(24,2),
  "asset_count" integer,
  "critical_assets" integer,
  "watch_assets" integer,
  "average_asset_health_score" numeric(24,2),
  "weather_forecast_quality_pct" numeric(24,2),
  "physics_model_fit_pct" numeric(24,2),
  "data_completeness_pct" numeric(24,2),
  "asset_state_reliability_pct" numeric(24,2),
  "prediction_confidence_pct" numeric(24,2),
  "confidence_method" varchar(1000),
  "weather_quality_method" varchar(1000),
  "cloud_average_method" varchar(200),
  "energy_method" varchar(200),
  "control_boundary" varchar(1000),
  created_at timestamptz not null default now(),
  updated_at timestamptz not null default now(),
  primary key (tenant_id, row_key)
);
comment on table aip."twin_forecast_governance" is 'Workbook sheet "Twin Forecast Governance"';
alter table aip."twin_forecast_governance" enable row level security;
drop policy if exists p_read on aip."twin_forecast_governance";
create policy p_read on aip."twin_forecast_governance" for select to authenticated using (aip.has_role(tenant_id));
drop policy if exists p_write on aip."twin_forecast_governance";
create policy p_write on aip."twin_forecast_governance" for all to authenticated
  using (aip.has_role(tenant_id, array['planner','admin'])) with check (aip.has_role(tenant_id, array['planner','admin']));
drop trigger if exists t_audit on aip."twin_forecast_governance";
create trigger t_audit after insert or update or delete on aip."twin_forecast_governance" for each row execute function aip.audit_row();
drop trigger if exists t_touch on aip."twin_forecast_governance";
create trigger t_touch before update on aip."twin_forecast_governance" for each row execute function aip.touch_updated_at();

-- Twin Loss Model · Forecast & Twin · business key: Plant_ID
create table if not exists aip."twin_loss_model" (
  tenant_id uuid not null references aip.tenants(id) on delete cascade,
  row_key text not null,
  "plant_id" varchar(200),
  "period" date,
  "gross_expected_mwh" numeric(24,2),
  "temperature_loss_mwh" numeric(24,2),
  "soiling_loss_mwh" numeric(24,2),
  "shading_loss_mwh" numeric(24,2),
  "clipping_loss_mwh" integer,
  "curtailment_loss_mwh" numeric(24,2),
  "availability_loss_mwh" numeric(24,2),
  "degradation_loss_mwh" numeric(24,2),
  "transformer_ac_loss_mwh" numeric(24,2),
  "residual_loss_mwh" numeric(24,2),
  "actual_mwh" numeric(24,2),
  "recoverable_mwh" numeric(24,2),
  "model_confidence_pct" numeric(24,2),
  "model_version" varchar(200),
  "weather_resource_loss_mwh" numeric(24,2),
  "weather_adjusted_expected_mwh" numeric(24,2),
  "plant_operational_loss_mwh" numeric(24,2),
  created_at timestamptz not null default now(),
  updated_at timestamptz not null default now(),
  primary key (tenant_id, row_key)
);
comment on table aip."twin_loss_model" is 'Workbook sheet "Twin Loss Model"';
alter table aip."twin_loss_model" enable row level security;
drop policy if exists p_read on aip."twin_loss_model";
create policy p_read on aip."twin_loss_model" for select to authenticated using (aip.has_role(tenant_id));
drop policy if exists p_write on aip."twin_loss_model";
create policy p_write on aip."twin_loss_model" for all to authenticated
  using (aip.has_role(tenant_id, array['planner','admin'])) with check (aip.has_role(tenant_id, array['planner','admin']));
drop trigger if exists t_audit on aip."twin_loss_model";
create trigger t_audit after insert or update or delete on aip."twin_loss_model" for each row execute function aip.audit_row();
drop trigger if exists t_touch on aip."twin_loss_model";
create trigger t_touch before update on aip."twin_loss_model" for each row execute function aip.touch_updated_at();

-- PLAN_Field_Packs · Planning & Optimization · business key: Field_Pack_ID
create table if not exists aip."plan_field_packs" (
  tenant_id uuid not null references aip.tenants(id) on delete cascade,
  row_key text not null,
  "field_pack_id" varchar(200),
  "intervention_id" varchar(200),
  "work_order_id" varchar(200),
  "asset_id" varchar(200),
  "checklist_id" varchar(200),
  "required_part_id" varchar(200),
  "required_tool_id" varchar(200),
  "permit_status" varchar(200),
  "manual_status" varchar(200),
  "ai_vision_link" varchar(200),
  "erp_eam_wo_status" varchar(200),
  "field_readiness" varchar(200),
  "source" varchar(200),
  created_at timestamptz not null default now(),
  updated_at timestamptz not null default now(),
  primary key (tenant_id, row_key)
);
comment on table aip."plan_field_packs" is 'Workbook sheet "PLAN_Field_Packs"';
alter table aip."plan_field_packs" enable row level security;
drop policy if exists p_read on aip."plan_field_packs";
create policy p_read on aip."plan_field_packs" for select to authenticated using (aip.has_role(tenant_id));
drop policy if exists p_write on aip."plan_field_packs";
create policy p_write on aip."plan_field_packs" for all to authenticated
  using (aip.has_role(tenant_id, array['planner','admin'])) with check (aip.has_role(tenant_id, array['planner','admin']));
drop trigger if exists t_audit on aip."plan_field_packs";
create trigger t_audit after insert or update or delete on aip."plan_field_packs" for each row execute function aip.audit_row();
drop trigger if exists t_touch on aip."plan_field_packs";
create trigger t_touch before update on aip."plan_field_packs" for each row execute function aip.touch_updated_at();

-- PLAN_Scenarios · Planning & Optimization · business key: Scenario_ID
create table if not exists aip."plan_scenarios" (
  tenant_id uuid not null references aip.tenants(id) on delete cascade,
  row_key text not null,
  "scenario_id" varchar(200),
  "intervention_id" varchar(200),
  "scenario_type" varchar(200),
  "proposed_start" timestamp without time zone,
  "proposed_end" timestamp without time zone,
  "created_by" varchar(200),
  "scenario_status" varchar(200),
  "source" varchar(200),
  created_at timestamptz not null default now(),
  updated_at timestamptz not null default now(),
  primary key (tenant_id, row_key)
);
comment on table aip."plan_scenarios" is 'Workbook sheet "PLAN_Scenarios"';
alter table aip."plan_scenarios" enable row level security;
drop policy if exists p_read on aip."plan_scenarios";
create policy p_read on aip."plan_scenarios" for select to authenticated using (aip.has_role(tenant_id));
drop policy if exists p_write on aip."plan_scenarios";
create policy p_write on aip."plan_scenarios" for all to authenticated
  using (aip.has_role(tenant_id, array['planner','admin'])) with check (aip.has_role(tenant_id, array['planner','admin']));
drop trigger if exists t_audit on aip."plan_scenarios";
create trigger t_audit after insert or update or delete on aip."plan_scenarios" for each row execute function aip.audit_row();
drop trigger if exists t_touch on aip."plan_scenarios";
create trigger t_touch before update on aip."plan_scenarios" for each row execute function aip.touch_updated_at();

-- PLAN_Job_Templates · Planning & Optimization · business key: Template_ID
create table if not exists aip."plan_job_templates" (
  tenant_id uuid not null references aip.tenants(id) on delete cascade,
  row_key text not null,
  "template_id" varchar(200),
  "asset_class" varchar(200),
  "intervention_type" varchar(200),
  "duration_hours" integer,
  "required_skill" varchar(200),
  "crew_qty" integer,
  "part_id" varchar(200),
  "part_qty" integer,
  "tool_id" varchar(200),
  "vehicle_class" varchar(200),
  "permit_required" varchar(200),
  "outage_required" varchar(200),
  "checklist_id" varchar(200),
  "reference" varchar(200),
  created_at timestamptz not null default now(),
  updated_at timestamptz not null default now(),
  primary key (tenant_id, row_key)
);
comment on table aip."plan_job_templates" is 'Workbook sheet "PLAN_Job_Templates"';
alter table aip."plan_job_templates" enable row level security;
drop policy if exists p_read on aip."plan_job_templates";
create policy p_read on aip."plan_job_templates" for select to authenticated using (aip.has_role(tenant_id));
drop policy if exists p_write on aip."plan_job_templates";
create policy p_write on aip."plan_job_templates" for all to authenticated
  using (aip.has_role(tenant_id, array['planner','admin'])) with check (aip.has_role(tenant_id, array['planner','admin']));
drop trigger if exists t_audit on aip."plan_job_templates";
create trigger t_audit after insert or update or delete on aip."plan_job_templates" for each row execute function aip.audit_row();
drop trigger if exists t_touch on aip."plan_job_templates";
create trigger t_touch before update on aip."plan_job_templates" for each row execute function aip.touch_updated_at();

-- PLAN_Operational_Feed · Planning & Optimization · business key: Feed_ID
create table if not exists aip."plan_operational_feed" (
  tenant_id uuid not null references aip.tenants(id) on delete cascade,
  row_key text not null,
  "feed_id" varchar(200),
  "plant_id" varchar(200),
  "feed_type" varchar(200),
  "observed_at" timestamp without time zone,
  "metric" varchar(200),
  "value" numeric(24,2),
  "unit" varchar(200),
  "status" varchar(200),
  "freshness_seconds" integer,
  "evidence_id" varchar(200),
  "source_system" varchar(200),
  "source_mode" varchar(200),
  created_at timestamptz not null default now(),
  updated_at timestamptz not null default now(),
  primary key (tenant_id, row_key)
);
comment on table aip."plan_operational_feed" is 'Workbook sheet "PLAN_Operational_Feed"';
alter table aip."plan_operational_feed" enable row level security;
drop policy if exists p_read on aip."plan_operational_feed";
create policy p_read on aip."plan_operational_feed" for select to authenticated using (aip.has_role(tenant_id));
drop policy if exists p_write on aip."plan_operational_feed";
create policy p_write on aip."plan_operational_feed" for all to authenticated
  using (aip.has_role(tenant_id, array['planner','admin'])) with check (aip.has_role(tenant_id, array['planner','admin']));
drop trigger if exists t_audit on aip."plan_operational_feed";
create trigger t_audit after insert or update or delete on aip."plan_operational_feed" for each row execute function aip.audit_row();
drop trigger if exists t_touch on aip."plan_operational_feed";
create trigger t_touch before update on aip."plan_operational_feed" for each row execute function aip.touch_updated_at();

-- PLAN_Execution_Feedback · Planning & Optimization · business key: Execution_Feedback_ID
create table if not exists aip."plan_execution_feedback" (
  tenant_id uuid not null references aip.tenants(id) on delete cascade,
  row_key text not null,
  "execution_feedback_id" varchar(200),
  "work_order_id" varchar(200),
  "plant_id" varchar(200),
  "asset_id" varchar(200),
  "planned_start" timestamp without time zone,
  "actual_start" timestamp without time zone,
  "planned_duration_hours" numeric(24,2),
  "actual_duration_hours" numeric(24,2),
  "date_variance_hours" integer,
  "duration_variance_pct" numeric(24,2),
  "conformance_status" varchar(200),
  "outcome_status" varchar(200),
  "source" varchar(200),
  created_at timestamptz not null default now(),
  updated_at timestamptz not null default now(),
  primary key (tenant_id, row_key)
);
comment on table aip."plan_execution_feedback" is 'Workbook sheet "PLAN_Execution_Feedback"';
alter table aip."plan_execution_feedback" enable row level security;
drop policy if exists p_read on aip."plan_execution_feedback";
create policy p_read on aip."plan_execution_feedback" for select to authenticated using (aip.has_role(tenant_id));
drop policy if exists p_write on aip."plan_execution_feedback";
create policy p_write on aip."plan_execution_feedback" for all to authenticated
  using (aip.has_role(tenant_id, array['planner','admin'])) with check (aip.has_role(tenant_id, array['planner','admin']));
drop trigger if exists t_audit on aip."plan_execution_feedback";
create trigger t_audit after insert or update or delete on aip."plan_execution_feedback" for each row execute function aip.audit_row();
drop trigger if exists t_touch on aip."plan_execution_feedback";
create trigger t_touch before update on aip."plan_execution_feedback" for each row execute function aip.touch_updated_at();

-- SUS_Emission_Factor_Config · Sustainability & Portfolio · business key: Factor_ID
create table if not exists aip."sus_emission_factor_config" (
  tenant_id uuid not null references aip.tenants(id) on delete cascade,
  row_key text not null,
  "factor_id" varchar(200),
  "factor_name" varchar(200),
  "value" numeric(24,2),
  "unit" varchar(200),
  "authority" varchar(200),
  "source_reference" varchar(200),
  "effective_from" date,
  "effective_to" date,
  "status" varchar(200),
  "governance_note" varchar(1000),
  created_at timestamptz not null default now(),
  updated_at timestamptz not null default now(),
  primary key (tenant_id, row_key)
);
comment on table aip."sus_emission_factor_config" is 'Workbook sheet "SUS_Emission_Factor_Config"';
alter table aip."sus_emission_factor_config" enable row level security;
drop policy if exists p_read on aip."sus_emission_factor_config";
create policy p_read on aip."sus_emission_factor_config" for select to authenticated using (aip.has_role(tenant_id));
drop policy if exists p_write on aip."sus_emission_factor_config";
create policy p_write on aip."sus_emission_factor_config" for all to authenticated
  using (aip.has_role(tenant_id, array['planner','admin'])) with check (aip.has_role(tenant_id, array['planner','admin']));
drop trigger if exists t_audit on aip."sus_emission_factor_config";
create trigger t_audit after insert or update or delete on aip."sus_emission_factor_config" for each row execute function aip.audit_row();
drop trigger if exists t_touch on aip."sus_emission_factor_config";
create trigger t_touch before update on aip."sus_emission_factor_config" for each row execute function aip.touch_updated_at();

-- SUS_Avoided_Emissions_Calc · Sustainability & Portfolio · business key: Calculation_ID
create table if not exists aip."sus_avoided_emissions_calc" (
  tenant_id uuid not null references aip.tenants(id) on delete cascade,
  row_key text not null,
  "calculation_id" varchar(200),
  "period" varchar(200),
  "plant_id" varchar(200),
  "plant_name" varchar(200),
  "net_generation_mwh" numeric(24,2),
  "factor_id" varchar(200),
  "emission_factor_t_co2e_per_mwh" numeric(24,2),
  "avoided_emissions_t_co2e" varchar(200),
  "generation_source" varchar(200),
  "factor_source" varchar(200),
  "calculation_formula" varchar(200),
  "audit_status" varchar(200),
  created_at timestamptz not null default now(),
  updated_at timestamptz not null default now(),
  primary key (tenant_id, row_key)
);
comment on table aip."sus_avoided_emissions_calc" is 'Workbook sheet "SUS_Avoided_Emissions_Calc"';
alter table aip."sus_avoided_emissions_calc" enable row level security;
drop policy if exists p_read on aip."sus_avoided_emissions_calc";
create policy p_read on aip."sus_avoided_emissions_calc" for select to authenticated using (aip.has_role(tenant_id));
drop policy if exists p_write on aip."sus_avoided_emissions_calc";
create policy p_write on aip."sus_avoided_emissions_calc" for all to authenticated
  using (aip.has_role(tenant_id, array['planner','admin'])) with check (aip.has_role(tenant_id, array['planner','admin']));
drop trigger if exists t_audit on aip."sus_avoided_emissions_calc";
create trigger t_audit after insert or update or delete on aip."sus_avoided_emissions_calc" for each row execute function aip.audit_row();
drop trigger if exists t_touch on aip."sus_avoided_emissions_calc";
create trigger t_touch before update on aip."sus_avoided_emissions_calc" for each row execute function aip.touch_updated_at();

-- PORT_Revenue_Risk_Calc · Sustainability & Portfolio · business key: Calculation_ID
create table if not exists aip."port_revenue_risk_calc" (
  tenant_id uuid not null references aip.tenants(id) on delete cascade,
  row_key text not null,
  "calculation_id" varchar(200),
  "period" varchar(200),
  "plant_id" varchar(200),
  "plant_name" varchar(200),
  "lost_generation_mwh" numeric(24,2),
  "tariff_rate_id" varchar(200),
  "tariff_per_k_wh" numeric(24,4),
  "lost_revenue" numeric(24,4),
  "penalty_included" numeric(24,2),
  "recoverable_included" numeric(24,2),
  "formula" varchar(200),
  "audit_status" varchar(200),
  currency char(3) not null default 'INR' check (currency ~ '^[A-Z]{3}$'),
  created_at timestamptz not null default now(),
  updated_at timestamptz not null default now(),
  primary key (tenant_id, row_key)
);
comment on table aip."port_revenue_risk_calc" is 'Workbook sheet "PORT_Revenue_Risk_Calc"';
alter table aip."port_revenue_risk_calc" enable row level security;
drop policy if exists p_read on aip."port_revenue_risk_calc";
create policy p_read on aip."port_revenue_risk_calc" for select to authenticated using (aip.has_role(tenant_id));
drop policy if exists p_write on aip."port_revenue_risk_calc";
create policy p_write on aip."port_revenue_risk_calc" for all to authenticated
  using (aip.has_role(tenant_id, array['planner','admin'])) with check (aip.has_role(tenant_id, array['planner','admin']));
drop trigger if exists t_audit on aip."port_revenue_risk_calc";
create trigger t_audit after insert or update or delete on aip."port_revenue_risk_calc" for each row execute function aip.audit_row();
drop trigger if exists t_touch on aip."port_revenue_risk_calc";
create trigger t_touch before update on aip."port_revenue_risk_calc" for each row execute function aip.touch_updated_at();

-- PNO_Rolling_Window_Config · Planning & Optimization · business key: Config_ID
create table if not exists aip."pno_rolling_window_config" (
  tenant_id uuid not null references aip.tenants(id) on delete cascade,
  row_key text not null,
  "config_id" varchar(200),
  "parameter" varchar(200),
  "value" varchar(200),
  "unit" varchar(200),
  "trigger" varchar(200),
  "governance_status" varchar(200),
  "governance_note" varchar(1000),
  created_at timestamptz not null default now(),
  updated_at timestamptz not null default now(),
  primary key (tenant_id, row_key)
);
comment on table aip."pno_rolling_window_config" is 'Workbook sheet "PNO_Rolling_Window_Config"';
alter table aip."pno_rolling_window_config" enable row level security;
drop policy if exists p_read on aip."pno_rolling_window_config";
create policy p_read on aip."pno_rolling_window_config" for select to authenticated using (aip.has_role(tenant_id));
drop policy if exists p_write on aip."pno_rolling_window_config";
create policy p_write on aip."pno_rolling_window_config" for all to authenticated
  using (aip.has_role(tenant_id, array['planner','admin'])) with check (aip.has_role(tenant_id, array['planner','admin']));
drop trigger if exists t_audit on aip."pno_rolling_window_config";
create trigger t_audit after insert or update or delete on aip."pno_rolling_window_config" for each row execute function aip.audit_row();
drop trigger if exists t_touch on aip."pno_rolling_window_config";
create trigger t_touch before update on aip."pno_rolling_window_config" for each row execute function aip.touch_updated_at();

-- PLAN_Resource_Calendar · Planning & Optimization · business key: Resource_Event_ID
create table if not exists aip."plan_resource_calendar" (
  tenant_id uuid not null references aip.tenants(id) on delete cascade,
  row_key text not null,
  "resource_event_id" varchar(200),
  "resource_type" varchar(200),
  "resource_id" varchar(200),
  "plant_id" varchar(200),
  "start" timestamp without time zone,
  "end" timestamp without time zone,
  "event_type" varchar(200),
  "availability" varchar(200),
  "available_hours" integer,
  "committed_hours" integer,
  "capacity_pct" integer,
  "skill_or_certification" varchar(200),
  "location" varchar(200),
  "reason" varchar(200),
  "source" varchar(200),
  created_at timestamptz not null default now(),
  updated_at timestamptz not null default now(),
  primary key (tenant_id, row_key)
);
comment on table aip."plan_resource_calendar" is 'Workbook sheet "PLAN_Resource_Calendar"';
alter table aip."plan_resource_calendar" enable row level security;
drop policy if exists p_read on aip."plan_resource_calendar";
create policy p_read on aip."plan_resource_calendar" for select to authenticated using (aip.has_role(tenant_id));
drop policy if exists p_write on aip."plan_resource_calendar";
create policy p_write on aip."plan_resource_calendar" for all to authenticated
  using (aip.has_role(tenant_id, array['planner','admin'])) with check (aip.has_role(tenant_id, array['planner','admin']));
drop trigger if exists t_audit on aip."plan_resource_calendar";
create trigger t_audit after insert or update or delete on aip."plan_resource_calendar" for each row execute function aip.audit_row();
drop trigger if exists t_touch on aip."plan_resource_calendar";
create trigger t_touch before update on aip."plan_resource_calendar" for each row execute function aip.touch_updated_at();

-- PLAN_Weather_Forecast · Planning & Optimization · business key: Weather_ID
create table if not exists aip."plan_weather_forecast" (
  tenant_id uuid not null references aip.tenants(id) on delete cascade,
  row_key text not null,
  "weather_id" varchar(200),
  "plant_id" varchar(200),
  "forecast_time" timestamp without time zone,
  "valid_from" timestamp without time zone,
  "valid_to" timestamp without time zone,
  "rain_probability_pct" integer,
  "rainfall_mm" numeric(24,2),
  "wind_kmh" integer,
  "gust_kmh" integer,
  "lightning_risk" varchar(200),
  "temperature_c" integer,
  "visibility_km" integer,
  "work_at_height_status" varchar(200),
  "outdoor_electrical_status" varchar(200),
  "access_status" varchar(200),
  "freshness_status" varchar(200),
  "source_system" varchar(200),
  "source_mode" varchar(200),
  "evidence_id" varchar(200),
  created_at timestamptz not null default now(),
  updated_at timestamptz not null default now(),
  primary key (tenant_id, row_key)
);
comment on table aip."plan_weather_forecast" is 'Workbook sheet "PLAN_Weather_Forecast"';
alter table aip."plan_weather_forecast" enable row level security;
drop policy if exists p_read on aip."plan_weather_forecast";
create policy p_read on aip."plan_weather_forecast" for select to authenticated using (aip.has_role(tenant_id));
drop policy if exists p_write on aip."plan_weather_forecast";
create policy p_write on aip."plan_weather_forecast" for all to authenticated
  using (aip.has_role(tenant_id, array['planner','admin'])) with check (aip.has_role(tenant_id, array['planner','admin']));
drop trigger if exists t_audit on aip."plan_weather_forecast";
create trigger t_audit after insert or update or delete on aip."plan_weather_forecast" for each row execute function aip.audit_row();
drop trigger if exists t_touch on aip."plan_weather_forecast";
create trigger t_touch before update on aip."plan_weather_forecast" for each row execute function aip.touch_updated_at();

-- PNO_Alternative_Config · Planning & Optimization · business key: Alternative_ID
create table if not exists aip."pno_alternative_config" (
  tenant_id uuid not null references aip.tenants(id) on delete cascade,
  row_key text not null,
  "alternative_id" varchar(200),
  "objective_name" varchar(200),
  "generation_priority" integer,
  "enabled" varchar(200),
  "maximum_per_run" integer,
  "duplicate_key" varchar(200),
  "governance_status" varchar(200),
  "governance_note" varchar(200),
  created_at timestamptz not null default now(),
  updated_at timestamptz not null default now(),
  primary key (tenant_id, row_key)
);
comment on table aip."pno_alternative_config" is 'Workbook sheet "PNO_Alternative_Config"';
alter table aip."pno_alternative_config" enable row level security;
drop policy if exists p_read on aip."pno_alternative_config";
create policy p_read on aip."pno_alternative_config" for select to authenticated using (aip.has_role(tenant_id));
drop policy if exists p_write on aip."pno_alternative_config";
create policy p_write on aip."pno_alternative_config" for all to authenticated
  using (aip.has_role(tenant_id, array['planner','admin'])) with check (aip.has_role(tenant_id, array['planner','admin']));
drop trigger if exists t_audit on aip."pno_alternative_config";
create trigger t_audit after insert or update or delete on aip."pno_alternative_config" for each row execute function aip.audit_row();
drop trigger if exists t_touch on aip."pno_alternative_config";
create trigger t_touch before update on aip."pno_alternative_config" for each row execute function aip.touch_updated_at();

-- PNO_Plan_Comparison_Config · Planning & Optimization · business key: Measure_ID
create table if not exists aip."pno_plan_comparison_config" (
  tenant_id uuid not null references aip.tenants(id) on delete cascade,
  row_key text not null,
  "measure_id" varchar(200),
  "measure" varchar(200),
  "baseline_required" varchar(200),
  "alternative_required" varchar(200),
  "direction" varchar(200),
  "governance_status" varchar(200),
  "decision_use" varchar(200),
  created_at timestamptz not null default now(),
  updated_at timestamptz not null default now(),
  primary key (tenant_id, row_key)
);
comment on table aip."pno_plan_comparison_config" is 'Workbook sheet "PNO_Plan_Comparison_Config"';
alter table aip."pno_plan_comparison_config" enable row level security;
drop policy if exists p_read on aip."pno_plan_comparison_config";
create policy p_read on aip."pno_plan_comparison_config" for select to authenticated using (aip.has_role(tenant_id));
drop policy if exists p_write on aip."pno_plan_comparison_config";
create policy p_write on aip."pno_plan_comparison_config" for all to authenticated
  using (aip.has_role(tenant_id, array['planner','admin'])) with check (aip.has_role(tenant_id, array['planner','admin']));
drop trigger if exists t_audit on aip."pno_plan_comparison_config";
create trigger t_audit after insert or update or delete on aip."pno_plan_comparison_config" for each row execute function aip.audit_row();
drop trigger if exists t_touch on aip."pno_plan_comparison_config";
create trigger t_touch before update on aip."pno_plan_comparison_config" for each row execute function aip.touch_updated_at();

-- Data Quality Checks · Governance & Technology · business key: Rule_ID
create table if not exists aip."data_quality_checks" (
  tenant_id uuid not null references aip.tenants(id) on delete cascade,
  row_key text not null,
  "rule_id" varchar(200),
  "check" varchar(200),
  "domain" varchar(200),
  "population" varchar(200),
  "required_fields" varchar(200),
  "rule_type" varchar(200),
  "threshold" integer,
  "kpi_use" varchar(200),
  "runtime_basis" varchar(200),
  "governance_status" varchar(200),
  created_at timestamptz not null default now(),
  updated_at timestamptz not null default now(),
  primary key (tenant_id, row_key)
);
comment on table aip."data_quality_checks" is 'Workbook sheet "Data Quality Checks"';
alter table aip."data_quality_checks" enable row level security;
drop policy if exists p_read on aip."data_quality_checks";
create policy p_read on aip."data_quality_checks" for select to authenticated using (aip.has_role(tenant_id));
drop policy if exists p_write on aip."data_quality_checks";
create policy p_write on aip."data_quality_checks" for all to authenticated
  using (aip.has_role(tenant_id, array['planner','admin'])) with check (aip.has_role(tenant_id, array['planner','admin']));
drop trigger if exists t_audit on aip."data_quality_checks";
create trigger t_audit after insert or update or delete on aip."data_quality_checks" for each row execute function aip.audit_row();
drop trigger if exists t_touch on aip."data_quality_checks";
create trigger t_touch before update on aip."data_quality_checks" for each row execute function aip.touch_updated_at();

-- AI Guardrail Policies · Governance & Technology · business key: Control_ID
create table if not exists aip."ai_guardrail_policies" (
  tenant_id uuid not null references aip.tenants(id) on delete cascade,
  row_key text not null,
  "control_id" varchar(200),
  "control" varchar(200),
  "domain" varchar(200),
  "initial_state" varchar(200),
  "evidence_source" varchar(200),
  "runtime_behaviour" varchar(1000),
  "gap_or_qualification" varchar(1000),
  "target_state" varchar(200),
  "dynamic_status_rule" varchar(1000),
  "evidence_keys" varchar(1000),
  "state_ownership" varchar(200),
  created_at timestamptz not null default now(),
  updated_at timestamptz not null default now(),
  primary key (tenant_id, row_key)
);
comment on table aip."ai_guardrail_policies" is 'Workbook sheet "AI Guardrail Policies"';
alter table aip."ai_guardrail_policies" enable row level security;
drop policy if exists p_read on aip."ai_guardrail_policies";
create policy p_read on aip."ai_guardrail_policies" for select to authenticated using (aip.has_role(tenant_id));
drop policy if exists p_write on aip."ai_guardrail_policies";
create policy p_write on aip."ai_guardrail_policies" for all to authenticated
  using (aip.has_role(tenant_id, array['planner','admin'])) with check (aip.has_role(tenant_id, array['planner','admin']));
drop trigger if exists t_audit on aip."ai_guardrail_policies";
create trigger t_audit after insert or update or delete on aip."ai_guardrail_policies" for each row execute function aip.audit_row();
drop trigger if exists t_touch on aip."ai_guardrail_policies";
create trigger t_touch before update on aip."ai_guardrail_policies" for each row execute function aip.touch_updated_at();

-- AI Guardrail Decisions · Governance & Technology · business key: Timestamp
create table if not exists aip."ai_guardrail_decisions" (
  tenant_id uuid not null references aip.tenants(id) on delete cascade,
  row_key text not null,
  "timestamp" varchar(200),
  "capability" varchar(200),
  "action" varchar(200),
  "decision" varchar(200),
  "policy" varchar(200),
  "reason" varchar(200),
  "override_by" varchar(200),
  "override_reason" varchar(200),
  "evidence_reference" varchar(200),
  "outcome" varchar(200),
  created_at timestamptz not null default now(),
  updated_at timestamptz not null default now(),
  primary key (tenant_id, row_key)
);
comment on table aip."ai_guardrail_decisions" is 'Workbook sheet "AI Guardrail Decisions"';
alter table aip."ai_guardrail_decisions" enable row level security;
drop policy if exists p_read on aip."ai_guardrail_decisions";
create policy p_read on aip."ai_guardrail_decisions" for select to authenticated using (aip.has_role(tenant_id));
drop policy if exists p_write on aip."ai_guardrail_decisions";
create policy p_write on aip."ai_guardrail_decisions" for all to authenticated
  using (aip.has_role(tenant_id, array['planner','admin'])) with check (aip.has_role(tenant_id, array['planner','admin']));
drop trigger if exists t_audit on aip."ai_guardrail_decisions";
create trigger t_audit after insert or update or delete on aip."ai_guardrail_decisions" for each row execute function aip.audit_row();
drop trigger if exists t_touch on aip."ai_guardrail_decisions";
create trigger t_touch before update on aip."ai_guardrail_decisions" for each row execute function aip.touch_updated_at();

-- FCST_Run · Forecast & Twin · business key: Forecast_Run_ID
create table if not exists aip."fcst_run" (
  tenant_id uuid not null references aip.tenants(id) on delete cascade,
  row_key text not null,
  "forecast_run_id" varchar(200),
  "plant_id" varchar(200),
  "forecast_run_timestamp" timestamp without time zone,
  "horizon_hours" integer,
  "forecast_start" timestamp without time zone,
  "forecast_end" timestamp without time zone,
  "resolution_minutes" integer,
  "physics_forecast_mwh" numeric(24,3),
  "hybrid_forecast_mwh" numeric(24,3),
  "operational_energy_at_risk_mwh" numeric(24,3),
  "calibrated_band_pct" numeric(24,3),
  "data_trust_pct" numeric(24,2),
  "model_reliability_n_mae_pct" numeric(24,3),
  "physics_model_id" varchar(200),
  "residual_model_id" varchar(200),
  "uncertainty_model_id" varchar(200),
  "run_status" varchar(200),
  "source_mode" varchar(200),
  created_at timestamptz not null default now(),
  updated_at timestamptz not null default now(),
  primary key (tenant_id, row_key)
);
comment on table aip."fcst_run" is 'Workbook sheet "FCST_Run"';
alter table aip."fcst_run" enable row level security;
drop policy if exists p_read on aip."fcst_run";
create policy p_read on aip."fcst_run" for select to authenticated using (aip.has_role(tenant_id));
drop policy if exists p_write on aip."fcst_run";
create policy p_write on aip."fcst_run" for all to authenticated
  using (aip.has_role(tenant_id, array['planner','admin'])) with check (aip.has_role(tenant_id, array['planner','admin']));
drop trigger if exists t_audit on aip."fcst_run";
create trigger t_audit after insert or update or delete on aip."fcst_run" for each row execute function aip.audit_row();
drop trigger if exists t_touch on aip."fcst_run";
create trigger t_touch before update on aip."fcst_run" for each row execute function aip.touch_updated_at();

-- FCST_Interval · Forecast & Twin · business key: Plant_ID + Interval_Start
create table if not exists aip."fcst_interval" (
  tenant_id uuid not null references aip.tenants(id) on delete cascade,
  row_key text not null,
  "forecast_run_timestamp" timestamp without time zone,
  "plant_id" varchar(200),
  "interval_start" timestamp without time zone,
  "interval_minutes" integer,
  "lead_hours" numeric(24,2),
  "clear_sky_poa_wm2" numeric(24,2),
  "forecast_poa_wm2" numeric(24,2),
  "cloud_cover_pct" numeric(24,2),
  "ambient_temp_c" numeric(24,2),
  "wind_speed_ms" numeric(24,2),
  "soiling_pct" numeric(24,2),
  "availability_pct" numeric(24,2),
  "curtailment_pct" numeric(24,2),
  "asset_health_score" integer,
  "clear_sky_ac_potential_mw" numeric(24,4),
  "weather_adjusted_physics_mw" numeric(24,4),
  "after_availability_mw" numeric(24,4),
  "after_curtailment_mw" numeric(24,4),
  "after_asset_state_mw" numeric(24,4),
  "ml_residual_pct" numeric(24,4),
  "ml_residual_correction_mw" numeric(24,4),
  "forecast_delivered_mw" numeric(24,4),
  "uncertainty_pct" numeric(24,3),
  "forecast_lower_mw" numeric(24,4),
  "forecast_upper_mw" numeric(24,4),
  "model_id" varchar(200),
  "physics_model_version" varchar(200),
  "residual_model_version" varchar(200),
  "data_source" varchar(200),
  "uncertainty_method" varchar(200),
  created_at timestamptz not null default now(),
  updated_at timestamptz not null default now(),
  primary key (tenant_id, row_key)
);
comment on table aip."fcst_interval" is 'Workbook sheet "FCST_Interval"';
alter table aip."fcst_interval" enable row level security;
drop policy if exists p_read on aip."fcst_interval";
create policy p_read on aip."fcst_interval" for select to authenticated using (aip.has_role(tenant_id));
drop policy if exists p_write on aip."fcst_interval";
create policy p_write on aip."fcst_interval" for all to authenticated
  using (aip.has_role(tenant_id, array['planner','admin'])) with check (aip.has_role(tenant_id, array['planner','admin']));
drop trigger if exists t_audit on aip."fcst_interval";
create trigger t_audit after insert or update or delete on aip."fcst_interval" for each row execute function aip.audit_row();
drop trigger if exists t_touch on aip."fcst_interval";
create trigger t_touch before update on aip."fcst_interval" for each row execute function aip.touch_updated_at();

-- FCST_Actual · Forecast & Twin · business key: Plant_ID + Interval_Start
create table if not exists aip."fcst_actual" (
  tenant_id uuid not null references aip.tenants(id) on delete cascade,
  row_key text not null,
  "plant_id" varchar(200),
  "interval_start" timestamp without time zone,
  "actual_ac_mw" numeric(24,4),
  "actual_energy_mwh" numeric(24,4),
  "observed_poa_wm2" numeric(24,2),
  "actual_availability_pct" numeric(24,3),
  "actual_curtailment_pct" numeric(24,2),
  "data_quality_status" varchar(200),
  "source_system" varchar(200),
  "source_mode" varchar(200),
  created_at timestamptz not null default now(),
  updated_at timestamptz not null default now(),
  primary key (tenant_id, row_key)
);
comment on table aip."fcst_actual" is 'Workbook sheet "FCST_Actual"';
alter table aip."fcst_actual" enable row level security;
drop policy if exists p_read on aip."fcst_actual";
create policy p_read on aip."fcst_actual" for select to authenticated using (aip.has_role(tenant_id));
drop policy if exists p_write on aip."fcst_actual";
create policy p_write on aip."fcst_actual" for all to authenticated
  using (aip.has_role(tenant_id, array['planner','admin'])) with check (aip.has_role(tenant_id, array['planner','admin']));
drop trigger if exists t_audit on aip."fcst_actual";
create trigger t_audit after insert or update or delete on aip."fcst_actual" for each row execute function aip.audit_row();
drop trigger if exists t_touch on aip."fcst_actual";
create trigger t_touch before update on aip."fcst_actual" for each row execute function aip.touch_updated_at();

-- FCST_Validation · Forecast & Twin · business key: Forecast_Run_ID + Interval_Start
create table if not exists aip."fcst_validation" (
  tenant_id uuid not null references aip.tenants(id) on delete cascade,
  row_key text not null,
  "forecast_run_id" varchar(200),
  "plant_id" varchar(200),
  "interval_start" timestamp without time zone,
  "lead_hours" numeric(24,2),
  "physics_forecast_mw" numeric(24,4),
  "hybrid_forecast_mw" numeric(24,4),
  "actual_mw" numeric(24,4),
  "physics_error_mw" numeric(24,4),
  "hybrid_error_mw" numeric(24,4),
  "physics_abs_error_pct" numeric(24,3),
  "hybrid_abs_error_pct" numeric(24,3),
  "inside_80_pct_band" varchar(200),
  "horizon_bucket_hours" integer,
  "validation_mode" varchar(200),
  created_at timestamptz not null default now(),
  updated_at timestamptz not null default now(),
  primary key (tenant_id, row_key)
);
comment on table aip."fcst_validation" is 'Workbook sheet "FCST_Validation"';
alter table aip."fcst_validation" enable row level security;
drop policy if exists p_read on aip."fcst_validation";
create policy p_read on aip."fcst_validation" for select to authenticated using (aip.has_role(tenant_id));
drop policy if exists p_write on aip."fcst_validation";
create policy p_write on aip."fcst_validation" for all to authenticated
  using (aip.has_role(tenant_id, array['planner','admin'])) with check (aip.has_role(tenant_id, array['planner','admin']));
drop trigger if exists t_audit on aip."fcst_validation";
create trigger t_audit after insert or update or delete on aip."fcst_validation" for each row execute function aip.audit_row();
drop trigger if exists t_touch on aip."fcst_validation";
create trigger t_touch before update on aip."fcst_validation" for each row execute function aip.touch_updated_at();

-- FCST_Model_Performance · Forecast & Twin · business key: Plant_ID + Horizon_Hours
create table if not exists aip."fcst_model_performance" (
  tenant_id uuid not null references aip.tenants(id) on delete cascade,
  row_key text not null,
  "scope" varchar(200),
  "plant_id" varchar(200),
  "horizon_hours" integer,
  "sample_count" integer,
  "physics_n_mae_pct" numeric(24,3),
  "hybrid_n_mae_pct" numeric(24,3),
  "hybrid_mae_mw" numeric(24,4),
  "hybrid_rmse_mw" numeric(24,4),
  "bias_mw" numeric(24,4),
  "improvement_vs_physics_pct" numeric(24,2),
  "interval_coverage_pct" numeric(24,2),
  "calibration_target_pct" integer,
  "model_id" varchar(200),
  "validation_basis" varchar(200),
  created_at timestamptz not null default now(),
  updated_at timestamptz not null default now(),
  primary key (tenant_id, row_key)
);
comment on table aip."fcst_model_performance" is 'Workbook sheet "FCST_Model_Performance"';
alter table aip."fcst_model_performance" enable row level security;
drop policy if exists p_read on aip."fcst_model_performance";
create policy p_read on aip."fcst_model_performance" for select to authenticated using (aip.has_role(tenant_id));
drop policy if exists p_write on aip."fcst_model_performance";
create policy p_write on aip."fcst_model_performance" for all to authenticated
  using (aip.has_role(tenant_id, array['planner','admin'])) with check (aip.has_role(tenant_id, array['planner','admin']));
drop trigger if exists t_audit on aip."fcst_model_performance";
create trigger t_audit after insert or update or delete on aip."fcst_model_performance" for each row execute function aip.audit_row();
drop trigger if exists t_touch on aip."fcst_model_performance";
create trigger t_touch before update on aip."fcst_model_performance" for each row execute function aip.touch_updated_at();

-- FCST_Model_Governance · Forecast & Twin · business key: Model_ID
create table if not exists aip."fcst_model_governance" (
  tenant_id uuid not null references aip.tenants(id) on delete cascade,
  row_key text not null,
  "model_id" varchar(200),
  "model_name" varchar(200),
  "model_type" varchar(200),
  "version" varchar(200),
  "owner_foundation" varchar(200),
  "training_start" date,
  "training_end" date,
  "training_record_count" integer,
  "feature_set" varchar(1000),
  "algorithm" varchar(200),
  "validation_method" varchar(200),
  "primary_metric" varchar(200),
  "validation_result" numeric(24,3),
  "champion_challenger" varchar(200),
  "approval_status" varchar(200),
  "last_trained" date,
  "next_review" date,
  "production_note" varchar(200),
  created_at timestamptz not null default now(),
  updated_at timestamptz not null default now(),
  primary key (tenant_id, row_key)
);
comment on table aip."fcst_model_governance" is 'Workbook sheet "FCST_Model_Governance"';
alter table aip."fcst_model_governance" enable row level security;
drop policy if exists p_read on aip."fcst_model_governance";
create policy p_read on aip."fcst_model_governance" for select to authenticated using (aip.has_role(tenant_id));
drop policy if exists p_write on aip."fcst_model_governance";
create policy p_write on aip."fcst_model_governance" for all to authenticated
  using (aip.has_role(tenant_id, array['planner','admin'])) with check (aip.has_role(tenant_id, array['planner','admin']));
drop trigger if exists t_audit on aip."fcst_model_governance";
create trigger t_audit after insert or update or delete on aip."fcst_model_governance" for each row execute function aip.audit_row();
drop trigger if exists t_touch on aip."fcst_model_governance";
create trigger t_touch before update on aip."fcst_model_governance" for each row execute function aip.touch_updated_at();

-- FCST_Commercial_Exposure · Forecast & Twin · business key: Exposure_ID
create table if not exists aip."fcst_commercial_exposure" (
  tenant_id uuid not null references aip.tenants(id) on delete cascade,
  row_key text not null,
  "exposure_id" varchar(200),
  "forecast_run_id" varchar(200),
  "plant_id" varchar(200),
  "horizon_hours" integer,
  "physics_baseline_mwh" numeric(24,3),
  "hybrid_delivered_mwh" numeric(24,3),
  "forecast_shortfall_mwh" numeric(24,3),
  "tariff_rate_id" varchar(200),
  "tariff_per_k_wh" numeric(24,4),
  "forward_revenue_exposure" numeric(24,2),
  "commercial_method" varchar(200),
  "audit_status" varchar(200),
  currency char(3) not null default 'INR' check (currency ~ '^[A-Z]{3}$'),
  created_at timestamptz not null default now(),
  updated_at timestamptz not null default now(),
  primary key (tenant_id, row_key)
);
comment on table aip."fcst_commercial_exposure" is 'Workbook sheet "FCST_Commercial_Exposure"';
alter table aip."fcst_commercial_exposure" enable row level security;
drop policy if exists p_read on aip."fcst_commercial_exposure";
create policy p_read on aip."fcst_commercial_exposure" for select to authenticated using (aip.has_role(tenant_id));
drop policy if exists p_write on aip."fcst_commercial_exposure";
create policy p_write on aip."fcst_commercial_exposure" for all to authenticated
  using (aip.has_role(tenant_id, array['planner','admin'])) with check (aip.has_role(tenant_id, array['planner','admin']));
drop trigger if exists t_audit on aip."fcst_commercial_exposure";
create trigger t_audit after insert or update or delete on aip."fcst_commercial_exposure" for each row execute function aip.audit_row();
drop trigger if exists t_touch on aip."fcst_commercial_exposure";
create trigger t_touch before update on aip."fcst_commercial_exposure" for each row execute function aip.touch_updated_at();

-- PLAN_Outcome_Validation · Planning & Optimization · business key: Outcome_Validation_ID
create table if not exists aip."plan_outcome_validation" (
  tenant_id uuid not null references aip.tenants(id) on delete cascade,
  row_key text not null,
  "outcome_validation_id" varchar(200),
  "execution_feedback_id" varchar(200),
  "work_order_id" varchar(200),
  "plant_id" varchar(200),
  "asset_id" varchar(200),
  "forecast_run_id" varchar(200),
  "predicted_avoided_loss_mwh" numeric(24,2),
  "predicted_value_protected" numeric(24,2),
  "actual_validated_recovery_mwh" numeric(24,2),
  "actual_value_realized" numeric(24,2),
  "execution_cost" numeric(24,2),
  "net_realized_value" numeric(24,2),
  "realization_pct" numeric(24,2),
  "outcome_confidence_pct" numeric(24,2),
  "variance_driver" varchar(200),
  "validation_status" varchar(200),
  "learning_feedback_status" varchar(200),
  "source_mode" varchar(200),
  currency char(3) not null default 'INR' check (currency ~ '^[A-Z]{3}$'),
  created_at timestamptz not null default now(),
  updated_at timestamptz not null default now(),
  primary key (tenant_id, row_key)
);
comment on table aip."plan_outcome_validation" is 'Workbook sheet "PLAN_Outcome_Validation"';
alter table aip."plan_outcome_validation" enable row level security;
drop policy if exists p_read on aip."plan_outcome_validation";
create policy p_read on aip."plan_outcome_validation" for select to authenticated using (aip.has_role(tenant_id));
drop policy if exists p_write on aip."plan_outcome_validation";
create policy p_write on aip."plan_outcome_validation" for all to authenticated
  using (aip.has_role(tenant_id, array['planner','admin'])) with check (aip.has_role(tenant_id, array['planner','admin']));
drop trigger if exists t_audit on aip."plan_outcome_validation";
create trigger t_audit after insert or update or delete on aip."plan_outcome_validation" for each row execute function aip.audit_row();
drop trigger if exists t_touch on aip."plan_outcome_validation";
create trigger t_touch before update on aip."plan_outcome_validation" for each row execute function aip.touch_updated_at();

-- FCST_DQ_Rules · Forecast & Twin · business key: Rule_ID
create table if not exists aip."fcst_dq_rules" (
  tenant_id uuid not null references aip.tenants(id) on delete cascade,
  row_key text not null,
  "rule_id" varchar(200),
  "check" varchar(200),
  "domain" varchar(200),
  "population" varchar(200),
  "required_fields" varchar(200),
  "rule_type" varchar(200),
  "threshold" integer,
  "kpi_use" varchar(200),
  "runtime_basis" varchar(200),
  "governance_status" varchar(200),
  created_at timestamptz not null default now(),
  updated_at timestamptz not null default now(),
  primary key (tenant_id, row_key)
);
comment on table aip."fcst_dq_rules" is 'Workbook sheet "FCST_DQ_Rules"';
alter table aip."fcst_dq_rules" enable row level security;
drop policy if exists p_read on aip."fcst_dq_rules";
create policy p_read on aip."fcst_dq_rules" for select to authenticated using (aip.has_role(tenant_id));
drop policy if exists p_write on aip."fcst_dq_rules";
create policy p_write on aip."fcst_dq_rules" for all to authenticated
  using (aip.has_role(tenant_id, array['planner','admin'])) with check (aip.has_role(tenant_id, array['planner','admin']));
drop trigger if exists t_audit on aip."fcst_dq_rules";
create trigger t_audit after insert or update or delete on aip."fcst_dq_rules" for each row execute function aip.audit_row();
drop trigger if exists t_touch on aip."fcst_dq_rules";
create trigger t_touch before update on aip."fcst_dq_rules" for each row execute function aip.touch_updated_at();

-- Warranty Register · Maintenance & Reliability · business key: Warranty_ID
create table if not exists aip."warranty_register" (
  tenant_id uuid not null references aip.tenants(id) on delete cascade,
  row_key text not null,
  "warranty_id" varchar(200),
  "asset_id" varchar(200),
  "asset_tag" varchar(200),
  "plant_id" varchar(200),
  "asset_class" varchar(200),
  "oem" varchar(200),
  "warranty_type" varchar(200),
  "warranty_start_date" date,
  "warranty_end_date" date,
  "coverage_type" varchar(200),
  "claim_notice_days" integer,
  "remaining_coverage_value" numeric(24,2),
  "rca_required" varchar(200),
  "work_order_required" varchar(200),
  "invoice_required" varchar(200),
  "current_status" varchar(200),
  currency char(3) not null default 'INR' check (currency ~ '^[A-Z]{3}$'),
  created_at timestamptz not null default now(),
  updated_at timestamptz not null default now(),
  primary key (tenant_id, row_key)
);
comment on table aip."warranty_register" is 'Workbook sheet "Warranty Register"';
alter table aip."warranty_register" enable row level security;
drop policy if exists p_read on aip."warranty_register";
create policy p_read on aip."warranty_register" for select to authenticated using (aip.has_role(tenant_id));
drop policy if exists p_write on aip."warranty_register";
create policy p_write on aip."warranty_register" for all to authenticated
  using (aip.has_role(tenant_id, array['planner','admin'])) with check (aip.has_role(tenant_id, array['planner','admin']));
drop trigger if exists t_audit on aip."warranty_register";
create trigger t_audit after insert or update or delete on aip."warranty_register" for each row execute function aip.audit_row();
drop trigger if exists t_touch on aip."warranty_register";
create trigger t_touch before update on aip."warranty_register" for each row execute function aip.touch_updated_at();

-- Warranty Claims · Maintenance & Reliability · business key: Claim_ID
create table if not exists aip."warranty_claims" (
  tenant_id uuid not null references aip.tenants(id) on delete cascade,
  row_key text not null,
  "claim_id" varchar(200),
  "warranty_id" varchar(200),
  "incident_id" varchar(200),
  "asset_id" varchar(200),
  "asset_tag" varchar(200),
  "plant_id" varchar(200),
  "claim_date" date,
  "failure_event_id" varchar(200),
  "claim_status" varchar(200),
  "claimed_amount" numeric(24,2),
  "approved_amount" numeric(24,2),
  "days_open" integer,
  "evidence_completeness_pct" integer,
  "notice_deadline" date,
  "oem_response_date" date,
  "rejection_reason" varchar(200),
  "next_action" varchar(200),
  currency char(3) not null default 'INR' check (currency ~ '^[A-Z]{3}$'),
  created_at timestamptz not null default now(),
  updated_at timestamptz not null default now(),
  primary key (tenant_id, row_key)
);
comment on table aip."warranty_claims" is 'Workbook sheet "Warranty Claims"';
alter table aip."warranty_claims" enable row level security;
drop policy if exists p_read on aip."warranty_claims";
create policy p_read on aip."warranty_claims" for select to authenticated using (aip.has_role(tenant_id));
drop policy if exists p_write on aip."warranty_claims";
create policy p_write on aip."warranty_claims" for all to authenticated
  using (aip.has_role(tenant_id, array['planner','admin'])) with check (aip.has_role(tenant_id, array['planner','admin']));
drop trigger if exists t_audit on aip."warranty_claims";
create trigger t_audit after insert or update or delete on aip."warranty_claims" for each row execute function aip.audit_row();
drop trigger if exists t_touch on aip."warranty_claims";
create trigger t_touch before update on aip."warranty_claims" for each row execute function aip.touch_updated_at();

-- MSI_ERP_Spares · Maintenance Spares · business key: Record_ID
create table if not exists aip."msi_erp_spares" (
  tenant_id uuid not null references aip.tenants(id) on delete cascade,
  row_key text not null,
  "record_id" varchar(200),
  "part_id" varchar(200),
  "part_description" varchar(200),
  "plant_id" varchar(200),
  "on_hand_qty" integer,
  "reserved_qty" integer,
  "quality_hold_qty" integer,
  "in_transit_qty" integer,
  "available_qty" integer,
  "reorder_point_qty" integer,
  "safety_stock_qty" integer,
  "lead_time_days" integer,
  "unit_cost" numeric(24,2),
  "required_qty_90_d" integer,
  "earliest_need_by" varchar(200),
  "criticality" varchar(200),
  "supplier_status" varchar(200),
  "erp_source" varchar(200),
  "source_record_id" varchar(200),
  "last_updated" varchar(200),
  "part_master_source" varchar(200),
  "demand_source" varchar(200),
  currency char(3) not null default 'INR' check (currency ~ '^[A-Z]{3}$'),
  created_at timestamptz not null default now(),
  updated_at timestamptz not null default now(),
  primary key (tenant_id, row_key)
);
comment on table aip."msi_erp_spares" is 'Workbook sheet "MSI_ERP_Spares"';
alter table aip."msi_erp_spares" enable row level security;
drop policy if exists p_read on aip."msi_erp_spares";
create policy p_read on aip."msi_erp_spares" for select to authenticated using (aip.has_role(tenant_id));
drop policy if exists p_write on aip."msi_erp_spares";
create policy p_write on aip."msi_erp_spares" for all to authenticated
  using (aip.has_role(tenant_id, array['planner','admin'])) with check (aip.has_role(tenant_id, array['planner','admin']));
drop trigger if exists t_audit on aip."msi_erp_spares";
create trigger t_audit after insert or update or delete on aip."msi_erp_spares" for each row execute function aip.audit_row();
drop trigger if exists t_touch on aip."msi_erp_spares";
create trigger t_touch before update on aip."msi_erp_spares" for each row execute function aip.touch_updated_at();

-- MSI_Part_Master · Maintenance Spares · business key: Part_ID
create table if not exists aip."msi_part_master" (
  tenant_id uuid not null references aip.tenants(id) on delete cascade,
  row_key text not null,
  "part_id" varchar(200),
  "part_name" varchar(200),
  "category" varchar(200),
  "reorder_point" integer,
  "max_level" integer,
  "lead_time_days" integer,
  "unit_cost" numeric(24,2),
  "criticality" varchar(200),
  "preferred_supplier" varchar(200),
  "storage_location" varchar(200),
  "replenishment_mode" varchar(200),
  "shelf_life_control" varchar(200),
  "source" varchar(200),
  currency char(3) not null default 'INR' check (currency ~ '^[A-Z]{3}$'),
  created_at timestamptz not null default now(),
  updated_at timestamptz not null default now(),
  primary key (tenant_id, row_key)
);
comment on table aip."msi_part_master" is 'Workbook sheet "MSI_Part_Master"';
alter table aip."msi_part_master" enable row level security;
drop policy if exists p_read on aip."msi_part_master";
create policy p_read on aip."msi_part_master" for select to authenticated using (aip.has_role(tenant_id));
drop policy if exists p_write on aip."msi_part_master";
create policy p_write on aip."msi_part_master" for all to authenticated
  using (aip.has_role(tenant_id, array['planner','admin'])) with check (aip.has_role(tenant_id, array['planner','admin']));
drop trigger if exists t_audit on aip."msi_part_master";
create trigger t_audit after insert or update or delete on aip."msi_part_master" for each row execute function aip.audit_row();
drop trigger if exists t_touch on aip."msi_part_master";
create trigger t_touch before update on aip."msi_part_master" for each row execute function aip.touch_updated_at();

-- MSI_Spare_Requirements · Maintenance Spares · business key: Requirement_ID
create table if not exists aip."msi_spare_requirements" (
  tenant_id uuid not null references aip.tenants(id) on delete cascade,
  row_key text not null,
  "requirement_id" varchar(200),
  "intervention_id" varchar(200),
  "work_order_id" varchar(200),
  "decision_id" varchar(200),
  "plant_id" varchar(200),
  "asset_id" varchar(200),
  "asset_tag" varchar(200),
  "maintenance_type" varchar(200),
  "part_id" varchar(200),
  "required_qty" integer,
  "required_by" varchar(200),
  "requirement_status" varchar(200),
  "requirement_source" varchar(200),
  "source_record_id" varchar(200),
  "as_of" varchar(200),
  created_at timestamptz not null default now(),
  updated_at timestamptz not null default now(),
  primary key (tenant_id, row_key)
);
comment on table aip."msi_spare_requirements" is 'Workbook sheet "MSI_Spare_Requirements"';
alter table aip."msi_spare_requirements" enable row level security;
drop policy if exists p_read on aip."msi_spare_requirements";
create policy p_read on aip."msi_spare_requirements" for select to authenticated using (aip.has_role(tenant_id));
drop policy if exists p_write on aip."msi_spare_requirements";
create policy p_write on aip."msi_spare_requirements" for all to authenticated
  using (aip.has_role(tenant_id, array['planner','admin'])) with check (aip.has_role(tenant_id, array['planner','admin']));
drop trigger if exists t_audit on aip."msi_spare_requirements";
create trigger t_audit after insert or update or delete on aip."msi_spare_requirements" for each row execute function aip.audit_row();
drop trigger if exists t_touch on aip."msi_spare_requirements";
create trigger t_touch before update on aip."msi_spare_requirements" for each row execute function aip.touch_updated_at();

-- MSI_Data_Quality · Maintenance Spares · business key: Check_ID
create table if not exists aip."msi_data_quality" (
  tenant_id uuid not null references aip.tenants(id) on delete cascade,
  row_key text not null,
  "check_id" varchar(200),
  "check" varchar(200),
  "result" varchar(200),
  "evidence" varchar(200),
  created_at timestamptz not null default now(),
  updated_at timestamptz not null default now(),
  primary key (tenant_id, row_key)
);
comment on table aip."msi_data_quality" is 'Workbook sheet "MSI_Data_Quality"';
alter table aip."msi_data_quality" enable row level security;
drop policy if exists p_read on aip."msi_data_quality";
create policy p_read on aip."msi_data_quality" for select to authenticated using (aip.has_role(tenant_id));
drop policy if exists p_write on aip."msi_data_quality";
create policy p_write on aip."msi_data_quality" for all to authenticated
  using (aip.has_role(tenant_id, array['planner','admin'])) with check (aip.has_role(tenant_id, array['planner','admin']));
drop trigger if exists t_audit on aip."msi_data_quality";
create trigger t_audit after insert or update or delete on aip."msi_data_quality" for each row execute function aip.audit_row();
drop trigger if exists t_touch on aip."msi_data_quality";
create trigger t_touch before update on aip."msi_data_quality" for each row execute function aip.touch_updated_at();

-- DATA_Hardening_Audit · Governance & Technology · business key: Audit_Item
create table if not exists aip."data_hardening_audit" (
  tenant_id uuid not null references aip.tenants(id) on delete cascade,
  row_key text not null,
  "audit_item" varchar(200),
  "before" integer,
  "after" integer,
  "status" varchar(200),
  "notes" varchar(200),
  created_at timestamptz not null default now(),
  updated_at timestamptz not null default now(),
  primary key (tenant_id, row_key)
);
comment on table aip."data_hardening_audit" is 'Workbook sheet "DATA_Hardening_Audit"';
alter table aip."data_hardening_audit" enable row level security;
drop policy if exists p_read on aip."data_hardening_audit";
create policy p_read on aip."data_hardening_audit" for select to authenticated using (aip.has_role(tenant_id));
drop policy if exists p_write on aip."data_hardening_audit";
create policy p_write on aip."data_hardening_audit" for all to authenticated
  using (aip.has_role(tenant_id, array['planner','admin'])) with check (aip.has_role(tenant_id, array['planner','admin']));
drop trigger if exists t_audit on aip."data_hardening_audit";
create trigger t_audit after insert or update or delete on aip."data_hardening_audit" for each row execute function aip.audit_row();
drop trigger if exists t_touch on aip."data_hardening_audit";
create trigger t_touch before update on aip."data_hardening_audit" for each row execute function aip.touch_updated_at();

grant select, insert, update, delete on all tables in schema aip to authenticated;
grant all on all tables in schema aip to service_role;
revoke insert, update, delete on aip.audit_log from authenticated;
revoke all on aip.license_clock from authenticated;
