{
  "name": "sustantix-aip",
  "private": true,
  "version": "7.32.0",
  "description": "Sustantix Asset Intelligence Platform — confidential, proprietary.",
  "license": "UNLICENSED",
  "packageManager": "pnpm@10.0.0",
  "engines": { "node": ">=20" },
  "scripts": {
    "build": "pnpm -r --filter ./packages/** run build && pnpm --filter @sustantix/aip-runtime run build",
    "test": "pnpm -r run test",
    "typecheck": "pnpm -r run typecheck"
  }
}
